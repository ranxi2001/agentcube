interface:
  display_name: "AgentCube PR Review"
  short_description: "Review AgentCube PRs with final-head evidence"
  default_prompt: "Use $agentcube-pr-review to audit this exact AgentCube PR head from its Issue contract through changed files, CI test discovery, architecture, lifecycle, and failure paths; run the final-head harness when the PR is declared ready."
