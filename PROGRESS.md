# PROGRESS.md

这个文件是给 Agent 循环用的短记忆，不是日报。每次开始工作先读这里，每次结束只更新关键状态，避免下一轮从零开始。

## Goal

当前主线：参与 AgentCube upstream 社区，跟进 PR #385 / #387，并围绕 agent-sandbox compatibility、Sleep/Resume、SnapStart、observability、SDK lifecycle 和 sandbox benchmark 找可验证、低重复的贡献点。

## Last Run

- 2026-07-03 #420 初始评论分析：当前无真人维护者技术评论。`volcano-sh-bot` 只是提示未 approve，需要 `docker/OWNERS` approver（`acsoto` / `YaoZengzeng`）。Copilot 在三个 Dockerfile 都说 `BUILDPLATFORM` 未先声明 `ARG BUILDPLATFORM` 可能为空，这是误报倾向：本地 buildx multi-arch smoke 和 fork push Docker build job 已成功，BuildKit automatic platform args 可用于 `FROM --platform=$BUILDPLATFORM`；只有在 stage 内 `RUN` 使用时才需要重新 `ARG BUILDPLATFORM`。Gemini 建议把 `ARG TARGETOS` / `ARG TARGETARCH` 移到 `go mod download` 后、`go build` 前以共享依赖下载层缓存，这有工程合理性但不是正确性问题；建议先等真人 reviewer，如果要采纳则作为小范围 Dockerfile-only update，重新跑 builder-stage smoke 和 push validation。不要自动回复或 push。
- 2026-07-03 用户确认后已创建 upstream PR #420：`https://github.com/volcano-sh/agentcube/pull/420`，title `ci: build multi-arch images with native Go builder`，base `volcano-sh:main`，head `ranxi2001:ci/native-build-platform-images`，commit `0f58bdb`，非 draft，mergeable，labels `kind/enhancement` / `size/XS`。PR 只改 `docker/Dockerfile`、`docker/Dockerfile.router`、`docker/Dockerfile.picod` 三行 builder `FROM --platform=$BUILDPLATFORM`，不改 release workflow、Helm chart metadata、matrix/cache 或 PicoD runtime layer。#416 不冲突，因为 #416 只改 `.github/workflows/build-push-release.yml`；#264 是相关但不同范围，当前 #264 改 `docker/Dockerfile.workloadmanager` 和 Makefile。创建前验证：`git diff --check upstream/main...HEAD`、`make build-all`、三个 Dockerfile 的 `docker buildx build --builder agentcube-day39 --platform linux/amd64,linux/arm64 --target builder --progress=plain .`，fork push validation `ranxi2001:ci/native-build-platform-images` commit `0f58bdb` 9/9 success。创建后 DCO、Approve workflows、codespell、golangci-lint、Python Lint、Python SDK Tests 已 success，build/e2e/codegen/copyright/coverage 正在跑；后续只跟踪 CI 和 review，不自动 push/comment。
- 2026-07-03 用户确认后已在 #419 追加 PR scope comment：`https://github.com/volcano-sh/agentcube/issues/419#issuecomment-4873610799`。评论明确第一阶段 PR 只做方向 1：三个 Go image Dockerfile 的 builder stage 改为 `$BUILDPLATFORM`，继续用 `TARGETOS` / `TARGETARCH` 交叉编译；不改 release trigger、artifact naming、Helm chart metadata、matrix、buildx cache，也不混入 PicoD runtime-layer 优化。Karmada-style prebuild pipeline、PicoD runtime-layer optimization、workflow matrix/cache 都留作 follow-up。当前暂无 maintainer 技术回复；等待 #419 方向反馈，再决定是否把 `ci/native-build-platform-images` 做成最小 PR。
- 2026-07-03 已核对 #419 评论和 timeline：当前 issue open，assignee 为 `@ranxi2001`，label 已是 `kind/enhancement`。有效方式是评论 `/kind enhancement`，由 `volcano-sh-bot` 添加 label；此前 `gh issue edit --add-label kind/enhancement` 因权限不足失败，`/label kind/enhancement` 也被 bot 拒绝。后续 AgentCube issue/PR 的 `kind/*` 标签优先用 `/kind ...`，不要误用 GitHub label API 或 `/label kind/...`。当前暂无 maintainer 技术回复；等待 #419 方向反馈，再决定是否把 `ci/native-build-platform-images` 做成最小 PR。
- 2026-07-03 用户确认后已创建 Day39 upstream enhancement issue #419：`https://github.com/volcano-sh/agentcube/issues/419`，标题 `Optimize multi-arch image build time in the release workflow`。内容包含 fork-only GitHub runner benchmark：Baseline run `28636642514` success，三镜像 buildx 命令合计 `1588s`，benchmark job wall time `1610s`，workloadmanager arm64 Go build `1404.5s`；Scheme A run `28636642076` success，三镜像 buildx 命令合计 `308s`，benchmark job wall time `331s`，workloadmanager arm64 Go build `169.4s`；总体 buildx 命令耗时提升 `5.16x`，workloadmanager arm64 Go build 提升 `8.29x`。PicoD Go build 从 `9.6s` 到 `0.9s`，但总耗时仍由 arm64 `apt-get install python3` `119.1s` 主导，作为 follow-up。原始日志归档到 `internship-reports/benchmarks/day39-release-image-build/github-benchmark/`，摘要在 `summary.md`。尝试给 #419 加 `kind/enhancement` label 失败：`ranxi2001 does not have the correct permissions to execute AddLabelsToLabelable`，因此当前 issue 无 label。下一步等待 maintainer 反馈；若方向认可，再把 `ci/native-build-platform-images` 整理成最小 PR，不自动发 PR。
- 2026-07-03 已按用户要求创建 Day39 fork-only GitHub runner 测评分支，不创建 self-PR，也不污染真实 Scheme A PR 分支。Baseline 分支 `/tmp/agentcube-image-build-baseline-benchmark` / `origin/ci/image-build-baseline-benchmark` 基于 `upstream/main 7f5a730`，commit `4e737fc ci: add image build benchmark workflow`，只新增 `.github/workflows/image-build-benchmark.yml`；benchmark run `https://github.com/ranxi2001/agentcube/actions/runs/28636642514`，job `84924315369`。Scheme A 测评分支 `/tmp/agentcube-image-build-scheme-a-benchmark` / `origin/ci/image-build-scheme-a-benchmark` 基于 `ci/native-build-platform-images a20ab07`，commit `60e5849 ci: add image build benchmark workflow`；benchmark run `https://github.com/ranxi2001/agentcube/actions/runs/28636642076`，job `84924313877`。临时 workflow 只做 `docker buildx build --platform linux/amd64,linux/arm64 --progress=plain .`，不登录 GHCR、不 push image、不 package Helm chart，并上传 `benchmark-logs/` artifact。普通 push CI 也会一起跑，但性能数据只看 `Image Build Benchmark`。下一步等两个 benchmark run 完成后下载 artifact / 读取 plain logs，提取三镜像总耗时和 arm64 Go build / PicoD runtime 瓶颈，再更新 Day39 和准备 issue 数据。
- 2026-07-03 按用户要求把 Day39 Scheme A 按 PR 标准整理成真实 topic branch：`/tmp/agentcube-image-build-validation` 已 rebase 到最新 `upstream/main 7f5a730`，commit `a20ab07 ci: build multi-arch images with native Go builder`，包含 DCO signoff，只改 `docker/Dockerfile`、`docker/Dockerfile.router`、`docker/Dockerfile.picod` 三行 builder `FROM --platform=$BUILDPLATFORM`，已推到 fork `origin/ci/native-build-platform-images`。该分支是未来 upstream PR 原型，不包含 fork-only benchmark workflow。push 后常规 push CI 9/9 success；build 记录是 `Agentcube CI Workflow / build`：`https://github.com/ranxi2001/agentcube/actions/runs/28636134337/job/84922780136`。注意这个 build 是常规 Docker build，不是 release workflow multi-arch benchmark。下一步如继续 Day39，创建 fork-only benchmark 分支比较 GitHub runner 上 baseline / Scheme A / 可选 Scheme B 的 buildx `cacheonly` 数据；不要创建 self-PR，不要把临时 workflow 混入真实 PR branch。
- 2026-07-03 Day39 已继续推进 Release Image 多架构构建性能优化：在 `/tmp/agentcube-image-build-validation` 基于 `upstream/main` 创建本地验证分支 `ci/native-build-platform-images`，只改 3 个 Dockerfile builder `FROM --platform=$BUILDPLATFORM`，用 buildx builder `agentcube-day39` 跑完三镜像 `linux/amd64,linux/arm64` smoke，日志归档到 `internship-reports/benchmarks/day39-release-image-build/`。结果：workloadmanager 成功，总耗时 `553.93s`，arm64 target Go build `linux/amd64->arm64 builder` `380.0s`；router 成功，总耗时 `339.75s`，arm64 target Go build `266.0s`；picod 成功，总耗时 `1160.87s`，arm64 target Go build `86.4s`，但 arm64 Ubuntu `apt-get install python3` runtime layer 用 `1111.1s` 且进程为 QEMU aarch64 Python/apt。Day39/TODO 已更新：Scheme A 证明最小 Dockerfile 改动可行，PicoD runtime 优化拆 follow-up；下一步准备 upstream issue exact title/body 给用户确认，不直接开 PR。
- 2026-07-03 用户确认后已直接回复 #416 Copilot 评论 `discussion_r3517086590`，回复链接 `https://github.com/volcano-sh/agentcube/pull/416#discussion_r3517099151`。回复内容说明固定 `0.0.0` 是有意作为 latest/development chart 稳定入口，避免 GHCR 累积 `0.0.0-main.<run_number>` 临时版本；fork `main` push 已验证 `agentcube-0.0.0.tgz` 和 `ghcr.io/ranxi2001/charts/agentcube:0.0.0` 成功；若 maintainer 更偏好 immutable per-run snapshot chart，再切到 `0.0.0-main.${{ github.run_number }}`。最新查询 #416 所有实际 checks 均 success：build、e2e、approve workflows、codegen、codespell、copyright、lint、Python lint、Python SDK tests、coverage、DCO；PR `mergeable=MERGEABLE`，等待 maintainer review / `/lgtm` / `/approve` / tide。
- 2026-07-03 #416 更新后 Copilot 评论：固定 `CHART_VERSION=0.0.0` 会让每次 main push 复用同一个 chart version/OCI tag，可能导致非唯一构建、客户端/registry 不拾取更新或 registry tag immutability 问题，并建议用 run number / short SHA 组成唯一 SemVer prerelease。已 triage 到 Day38：这是有效 tradeoff，但不是 maintainer 结论；当前 `0.0.0` 是有意作为 latest/development chart 稳定入口，且用户此前不希望 run-number 临时版本污染 GHCR。暂不因 Copilot 单独改代码；若 maintainer 明确要求 immutable per-run snapshot chart，再切到 `0.0.0-main.${{ github.run_number }}` 等合法 prerelease。已准备未发布回复草稿。
- 2026-07-03 用户确认“更新吧，先把 #416 做完”。已更新 upstream PR #416：force-with-lease 把 `origin/ci/fix-release-chart-version` 从旧 tag-only commit `a6c4a82` 更新到 `427e618 ci: use valid chart version for latest releases`；PR title 改为 `ci: use valid chart version for latest releases`；PR body 改为保留 `main` trigger/latest image 发布、`main` chart `CHART_VERSION=0.0.0`、tag release 保留 `vX.Y.Z` 命名，并加入 fork run `28633043101` 成功验证数据；已回复 `zhzhuang-zju` review comment，说明不再移除 trigger。查询时 PR `mergeable=MERGEABLE`，DCO、Approve workflows、codespell、golangci-lint、Python lint、Python SDK tests 已成功，build/e2e/codegen/copyright/coverage 正在重新跑。不要再自动 push/comment；下一步只跟踪 #416 checks 和 maintainer review。`origin/main` 仍是临时测试 commit `427e618`，后续需恢复干净 mirror，但恢复会触发旧 upstream workflow，需等 #416 合并或准备取消 run。
- 2026-07-03 fork run `28633043101` 已完成并 success：repo `ranxi2001/agentcube`，event `push`，branch `main`，head `427e618eaec0749736a430ce8adcf7f9d075b783`，job `84913711568` 从 `2026-07-03T01:47:11Z` 到 `2026-07-03T02:12:20Z`。#416 新方案真实验证通过：`TAG=latest`、`CHART_VERSION=0.0.0`、`APP_VERSION=latest`；`agentcube-0.0.0.tgz` package 成功，`ghcr.io/ranxi2001/charts/agentcube:0.0.0` push 成功，latest images 也发布成功。性能数据同步进 Day38/Day39/TODO：`Build and push images` 用 `1487.1s`，`workloadmanager` 约 21m56s，其中 `linux/arm64 go build ./cmd/workload-manager` 用 `1291.4s`；router 约 39s，picod 约 2m12s，picod arm64 `apt-get install python3` 用 `120.0s`。下一步如用户确认，可更新 upstream PR #416 分支/标题/body/review reply；尚未推 open PR，也未回复 reviewer。`origin/main` 仍是临时测试 commit `427e618`，不是干净 mirror，恢复时注意会触发旧 upstream workflow。
- 2026-07-03 用户建议 Release Image 多架构构建性能优化先本地验证不同方案，再用数据支撑 upstream issue。已按 AgentCube issue/PR 规范补充 Day39：GitHub issue/PR 搜索暂未发现专门讨论 buildx/arm64 构建性能的 open issue，但 open PR #264 正在调整 workloadmanager build / image build 入口，后续 issue/PR 必须显式避开或引用它。Day39 已新增 issue-first 验证矩阵：baseline、Scheme A `FROM --platform=$BUILDPLATFORM`、Scheme B Karmada-style prebuild、可选 matrix/cache，并附英文 enhancement issue 草稿。fork run `28633043101` 后续已完成，最终结论见上一条。
- 2026-07-03 已新增 Day39 `internship-reports/day39-karmada-image-build-and-agentcube-buildx-performance-optimization.md`，分析 Karmada image build：`dockerhub-latest-image.yml` 用 matrix 对 11 个 target 并行执行 `make mp-image-*`；Makefile 先 `make target GOOS=linux GOARCH=amd64` 和 `GOARCH=arm64`，`hack/build.sh` 在宿主 runner 上 `CGO_ENABLED=0 GOOS/GOARCH go build` 输出 `_output/bin/<platform>/<target>`，再由 `hack/docker.sh` 用 runtime-only `cluster/images/buildx.Dockerfile` 执行 buildx，`COPY ${TARGETPLATFORM}/${BINARY}`。关键结论：Karmada 避免了在 QEMU arm64 builder stage 中跑 Go compiler。Day39 推荐 AgentCube 后续独立 PR 先做最小优化：3 个 Dockerfile builder 改为 `FROM --platform=$BUILDPLATFORM ... AS builder`，保持 `GOOS=${TARGETOS} GOARCH=${TARGETARCH}` 交叉编译，避免 QEMU 执行 Go compiler；更大的 Karmada-style 预编译二进制 + runtime-only Dockerfile / matrix / cache 作为 follow-up。README/TODO 已加 Day39 入口，TODO 中 #416 旧 tag-only 口径已修正为 latest image + chart `0.0.0`。fork run `28633043101` 截至本记录仍在 `build-and-push`，未进入 Helm 结果；`origin/main` 仍是临时测试 commit `427e618`。
- 2026-07-03 按用户要求临时把 fork `origin/main` 从旧测试 commit `bfbcab9` force-with-lease 推到 PR #416 新验证 commit `427e618`，触发 fork run `28633043101`（`Build and Push Release Images`）验证 latest image + chart `0.0.0`。截至本记录，该 run 仍在 `build-and-push` job 中，GitHub 对运行中 job 不提供完整日志；不要误判成 Helm 修复卡住。已分析 upstream 旧 job `28582345874` / job `84745444791`：总耗时 27m19s，其中 `Build and push images` 从 10:12:20 到 10:39:20 用 27m，`Package Helm chart` 是最后 <1s 失败。真正慢点是第一个 `workloadmanager` multi-arch 镜像的 `linux/arm64 go build ./cmd/workload-manager`，日志显示 `1415.0s`；根因是 GitHub x86 runner 上 buildx/QEMU 执行 arm64 builder stage。Router 后续大量复用缓存约 42s，Picod 约 2m18s，Picod 慢点是 arm64 Ubuntu runtime `apt-get install python3` 约 127s。Day38 已补分析。后续如果继续验证，需要看 fork run `28633043101` 结果；完成后处理 `origin/main` 是否恢复干净镜像，注意恢复到 `upstream/main` 会再次触发旧 broken release workflow，需谨慎或立即取消对应 run。
- 2026-07-03 PR #416 收到 `zhzhuang-zju` review：不应移除 `main` trigger，因为 latest image 仍需上传。已重新收敛方案：保留 `main` push release workflow，拆分 Docker image tag 和 Helm chart metadata；`main` push 使用 `TAG=latest`、`CHART_VERSION=0.0.0`、`APP_VERSION=latest`，release tag 保留现有 `vX.Y.Z` 命名。干净本地 PR worktree `/tmp/agentcube-pr416-final` 基于 `upstream/main 7cfeb8c`，单 commit `427e618 ci: use valid chart version for latest releases`；验证通过 `git diff upstream/main --check`、`actionlint -ignore 'too old to run on GitHub Actions' .github/workflows/build-push-release.yml`、`helm lint manifests/charts/base`、Helm package simulations for `0.0.0` / `v1.2.3` / `v1.2.3-alpha`。尚未 push 更新 open PR #416，也未回复 review；需要用户确认 exact diff、PR body、review reply 后再执行 force-with-lease 和 comment reply。Day38 已标记旧 tag-only 方案废弃并记录新方案。
- 2026-07-02 已按用户要求创建本地通用开发技能仓库 `C:/Users/ranxi/Desktop/Project/Onefly-Dev-Skills`，结构模仿 `Onefly-Content-Skills`：每个 skill 直接位于仓库根目录。初始提交 `4e8d236 feat: add generic development skills`，包含 `project2wsl`、`github-issue-discussion`、`github-pr-management`、`llm-e2e-test`、`sandbox-runtime-smoke`、`drawio-skill` 六个通用技能；没有复制 `.env`、pem、token、credentials、settings.local.json 等敏感/本地文件。`project2wsl` 已收敛为“最终交付自动打开 VS Code 的 WSL 项目”，常用命令是 `bash project2wsl/scripts/project2wsl.sh launch <project-name>`。
- 2026-07-02 已按用户要求扩展 `internship-reports/windows-wsl-development-environment-setup.md`：补充 WSL 正确目录判断、`~/projects` 统一项目目录、clone vs copy 取舍、复制 Windows 旧项目到 WSL、`rsync` 大项目迁移、打开其他项目、打开单个文件、`dev`/`ac`/`codeac`/`cdev`/`mkdev` 快捷方式，以及 `\\wsl.localhost\Ubuntu\home\ranxi\projects` 查看路径。核心规则：日常开发只打开 `/home/ranxi/projects/...`，不要继续在 `/mnt/c/...` 工作。
- 2026-07-02 已重新更新 upstream PR #416 body，修正此前残留的 Helm chart `vX.Y.Z -> X.Y.Z` normalization 误导描述。当前 PR 正文明确：release tag 下继续使用 `v1.2.3` 作为 Docker image tag、Helm chart `version`、`appVersion`、`.tgz` 文件名和 OCI tag；唯一发布行为变化是 `main` push 不再触发 `Build and Push Release Images`。本机 WSL 开发环境已配置：Ubuntu 24.04.2 LTS / WSL2，Go 1.26.4，make/gcc/g++，jq，Python 3.12，Docker CLI，Helm v4.2.2，kubectl v1.36.2，VS Code Remote - WSL；已新增迁移笔记 `internship-reports/windows-wsl-development-environment-setup.md` 并在 `internship-reports/README.md` 建入口。后续迁移项目到 WSL 推荐路径 `~/projects/agentcube`，用 `code .` 从 WSL 打开。
- 2026-07-02 用户不希望 PR #416 改变 Helm chart package / OCI tag 从 `vX.Y.Z` 到 `X.Y.Z`，并要求直接 force push 成单 commit。已在 `/tmp/agentcube-pr416-scope-fix` 将 PR 分支重写为单 commit `a6c4a82 ci: publish release artifacts only for tags`，并 `--force-with-lease` 更新 `origin/ci/fix-release-chart-version`（`bfbcab9...a6c4a82`）。最终 diff 只移除 `Build and Push Release Images` 的 `main` trigger，并保持 tag release 下 Docker image tag、Helm chart `version`、`appVersion`、`.tgz` 文件名都继续使用原始 `TAG=vX.Y.Z`。PR #416 body 已通过 REST 更新，删除无 `v` chart package normalization 说法；Day38 已同步最终记录。
- 2026-07-02 已处理本地 `intern` 拉取 `origin/intern` 时留下的 merge conflict：冲突文件采用 `origin/intern 8e79897` 的较新记录版本，保留 Day33/Day35 内容并合入 Day34-Day38、PR #414/#415/#416、push CI skill 更新；CRD 设计文档顺手清理 trailing whitespace，便于 `git diff --check` 通过。
- 2026-07-02 用户看到 `This branch is 162 commits ahead of and 10 commits behind volcano-sh/agentcube:main`，定位为 `origin/intern` 相对 `upstream/main` 的状态；`origin/main` 已是 `upstream/main 7cfeb8c` 的干净镜像。已在本地将 `intern` rebase 到最新 `upstream/main`，rebase 后本地 `intern` 相对 `upstream/main` 为 `behind=0 ahead=162`，准备 force-with-lease 推回 `origin/intern`。
- 2026-07-02 PR #416 创建时的初版 commit `bfbcab9` 曾把 Helm chart version 从 `vX.Y.Z` 改为 `X.Y.Z`，该方向已被用户否定并由最终单 commit `a6c4a82` 取代。后续不要再按 `CHART_VERSION=${TAG#v}` 或 `agentcube-1.2.3.tgz` 口径解释 #416；Day38 已记录这条收敛过程。
- 2026-07-02 Day38 `internship-reports/day38-release-image-ci-helm-chart-version-failure-analysis.md` 已补 PR #416 链接和最终口径：这不是 #415/#414 引入，而是 release workflow 长期问题；最终 PR 只停止 `main` merge 发布镜像/chart，release tag 下保留现有 `vX.Y.Z` chart package / OCI tag 命名。Day38 已附英文 PR 文稿，body 使用自然 GitHub Markdown、不手动硬换行。
- 2026-07-02 用户指出 PR body 不应按终端宽度手动硬换行。已将 upstream PR #415 body 改为自然 GitHub Markdown 段落，不改代码、不触发新 CI；`gh pr edit` 因 token 缺 `read:org/read:discussion` 在 GraphQL 查询阶段失败，随后使用 REST `PATCH /repos/volcano-sh/agentcube/issues/415` 成功更新。已更新 `.agents/skills/agentcube-pr-management/SKILL.md`：以后 PR body、issue body、upstream comment 的普通 prose 不再手动 80 列换行，提交给用户确认前必须检查 raw Markdown 中的意外段内换行。
- 2026-07-02 已新增 Day37 `internship-reports/day37-docs-proposal-directory-management.md`：调研 AgentCube 过去的提案主要散落在 `docs/design/`，`CONTRIBUTING.md` 也写重大设计走 `docs/design/`，但缺少 README 索引和模板；对比 Karmada `docs/proposals`，其有 proposal-template 和 topic 目录，但没有顶层 README 索引。已从最新 `upstream/main f9c37d5` 创建干净 worktree `/tmp/agentcube-proposals-index` 和分支 `docs/proposals-management`，当前 commit `8e8c455 docs: add proposal index and template`，新增 `docs/proposals/README.md`、`docs/proposals/proposal-template.md`，更新 `CONTRIBUTING.md`，并 force-with-lease 推送到 `origin/docs/proposals-management`。按用户反馈，README 不维护 `docs/proposals/` 下新增 proposal 的全量索引，只索引遗留在 `docs/design/` 的旧提案；new proposal layout 改为统一目录式 `docs/proposals/<proposal-name>/README.md`，不在初始阶段规划 `<area>/` 二级目录。Day37 已补充为什么 AgentCube 模板不完全照搬 Karmada：不引入编号流程、增加 status/last-updated/tracking-issue、Test Plan 顶层化、Implementation Plan 支持多 PR 落地；并已修正 template/README 里 optional 标注，`tracking-issue`、`User Stories`、`Implementation Plan` 均显式标为 optional。用户确认后已创建 upstream PR #415：`https://github.com/volcano-sh/agentcube/pull/415`，title `docs: add proposal index and template`，base `volcano-sh:main`，head `ranxi2001:docs/proposals-management`。已按 Gemini 评论补齐 README 模板清单中的 `risks and mitigations` 并 force-with-lease 更新 PR。PR open、非 draft、mergeable，label `kind/documentation`，DCO success，push 后 checks 重新运行。后续不要自动 push/comment；若 maintainer 反馈，先分类是否属于 PR scope，再让用户确认 exact update/comment。
- 2026-07-02 已新增 Day36 `internship-reports/day36-k8s-slow-resource-control-plane-design.md`：按当前项目管理分工，明确 node-ctl / sandbox-ctl / microVM / cache 快路径暂由其他人负责，本轮聚焦 K8s 生态内慢资源控制面。文档用 Mermaid 展开快慢资源双轨体系、`SandboxPoolTemplate` 全局策略 CRD、`SandboxPool` 节点实例 CRD、Template Controller / Pool Controller 职责边界、创建 / 更新 / override / 故障恢复流程、状态机、状态所有权、node-ctl 最小接口契约和后续 fake controller / placeholder spike 落地切片。README/TODO 已补 Day36 入口。
- 2026-07-02 已按用户要求在 Day32 `internship-reports/day32-substrate-competitive-analysis-and-agentcube-prd.md` 文末追加“Day35 新版本设计后的 PRD 修订”：将 Day32 标记为历史版 / v1 设计，保留 Substrate 对比、Session lifecycle、Router activation、Store CAS、RuntimeProvider 和 benchmark/conformance 思路，同时明确过期点和 Day35 后的新主线：K8s CRD 资源池、占位 Pod、node-local lifecycle、microVM、run-builder、L1/L2/L3 缓存、E2B-like SDK Facade。TODO 的 Day32 里程碑也已同步说明。
- 2026-07-02 已按用户要求整理 Day35 架构材料：`Agentcube新架构方案.png` -> `internship-reports/day35-agentcube-new-architecture-proposal.png`；外部参考 CRD 设计文档不作为 Day35 日报文件包装，已移动到 `docs/design/k8s-crd-sandbox-resource-pool-lifecycle-control-design.md` 并去掉 Day35 头部说明。新增 `internship-reports/day35-agentcube-architecture-iteration-conclusion.md`，记录三期会议迭代后的 AgentCube 新架构结论：K8s 管慢资源和资源边界，node-ctl / sandbox-ctl 管节点本地高频生命周期，microVM + run-builder + L1/L2/L3 缓存支撑低延迟和高密度，E2B-compatible 只作为后续 facade / conformance 方向。README/TODO 已补入口。
- 2026-07-02 已按用户追问在 Day35 总体架构七层表中标注 E2B-like SDK 兼容位置：主入口放在接入层的 `E2B-like SDK Facade`，负责把 E2B SDK / REST 调用翻译成 AgentCube 内部 API；完整兼容还依赖路由层 lifecycle、沙箱运行层 commands/files/process、存储缓存层 template/snapshot。
- 2026-07-02 用户确认后已创建 upstream PR #414：`https://github.com/volcano-sh/agentcube/pull/414`，title `ci: run validation workflows on branch push`，base `volcano-sh:main`，head `ranxi2001:ci/enable-push-validation`，commit `bb2a6e5`。PR open、非 draft、mergeable，labels `kind/enhancement` / `size/M`，DCO、Approve Workflows、build、e2e、codegen、codespell、copyright、lint、Python lint、Python SDK Tests、coverage 全部 success；tide pending 仅因缺 `approved` / `lgtm`。Day34/TODO 已补 PR URL、最终 checks 和跟踪原则。后续不要自动 push/comment；若 maintainer 反馈，先分类是否属于本 PR scope，再让用户确认 exact update/comment。
- 2026-07-02 已完成 Day34 AgentCube push CI upstream PR 准备：fork `main` 已同步到 `upstream/main d3eb47a`，clean worktree `/tmp/agentcube-push-ci-clean` 分支 `ci/enable-push-validation` 基于最新 upstream，只提交 `bb2a6e5 ci: run validation workflows on branch push`。该 commit 给 9 个验证 workflow 增加 Karmada-style `push.branches-ignore: ['dependabot/**']`，未改 release/publish/approve workflow；已 push 到 fork 并触发 9/9 push workflow 全部成功。Day34 报告 `internship-reports/day34-agentcube-push-ci-workflow-pr-prep.md` 已写清方案、9 vs 11 checks 解释和英文 PR 草稿。下一步如要开 upstream PR，先让用户确认 exact title/body/target：`ranxi2001:ci/enable-push-validation` -> `volcano-sh:main`。
- 2026-07-02 根据用户反馈改成真正 Karmada-style push CI：不再推荐永久新增独立 `branch-push-validation.yml`，而是在临时 worktree `/tmp/agentcube-push-ci-worktree` 的同一分支 `ci/branch-push-validation` 上重置回 `upstream/main 14a3f04` 后，改为给 9 个现有验证 workflow 增加 `push.branches-ignore: ["dependabot/**"]`：`main.yml`、`lint.yml`、`codegen-check.yml`、`e2e.yml`、`codespell.yml`、`copyright-check.yml`、`python-lint.yml`、`python-sdk-tests.yml`、`test-coverage.yml`。新 commit `9516ee5 ci: run validation workflows on branch push` 已 force-with-lease 推到 fork 分支；GitHub Actions 对该 commit 触发了 9 个现有 workflow 的 `event=push` run，全部成功：Python SDK Tests、Codespell、Python Lint、Codegen Check、Copyright Check、Lint、Agentcube CI Workflow、Test Coverage、Agentcube E2E Tests。结论：这比 `c7e8249` 单独 workflow 更好，因为 push 和 PR 复用同一批 workflow 文件，避免 CI 定义漂移。下一步如要 upstream PR，使用 `9516ee5` 方案，不使用已废弃的 `c7e8249` 单独 workflow 方案。
- 2026-07-02 已在独立 worktree `/tmp/agentcube-push-ci-worktree` 基于 `upstream/main 14a3f04` 起草 Karmada-style AgentCube branch push CI，分支 `ci/branch-push-validation`，commit `c7e8249 ci: add branch push validation workflow`，新增 `.github/workflows/branch-push-validation.yml`。设计为只触发 `push`（排除 `dependabot/**`），`permissions: contents: read`，按 `github.ref` 做 concurrency cancel，覆盖 Go fmt/non-e2e/race coverage、lint/codegen、build-all+三镜像 build、Python lint/SDK tests、codespell/copyright、e2e。已 push 到 fork 验证，不创建 PR；第一次 `e73a491` 失败在 spelling-copyright，因为 codespell 临时删除 `pyproject.toml/package*.json` 后未 restore 就跑 copyright；已补 restore 并 force-with-lease 到 `c7e8249`。GitHub Actions run `28560174001` 全绿，包含 e2e 成功。已同步修复本地 fork-push template 的同类 restore 缺口。若要上游化，需用户确认 exact diff/body 后再开 PR。
- 2026-07-02 已把用户提供的 `internship-reports/day33-e2b-architecture-protocol-overview.png` 插入 Day33 `internship-reports/day33-e2b-protocol-and-agent-era-docker-study.md` 的“图解总览”部分；图用于快速建立 E2B SDK/Gateway/Sandbox/基础设施/生态关系，正文仍以 OpenAPI、envd proto 和 SDK 源码拆解为准。
- 2026-07-02 已按“图片相关资料前面加 day 前缀、week 文件把 week 放前面”的规则重命名实习报告资产：Day28 Substrate counter 图、Day28 AgentCube session runtime 图和 Day33 E2B 全景图都已改为 `dayXX-...` 命名，`agentcube week1.md` 已改为 `week1-agentcube-work-plan-and-mentor-notes.md`，并同步更新引用。Day33 还补入“云大厂为什么会拥抱 E2B-like 抽象”的分析：云厂商拥抱的是 agent sandbox 默认接口层，因为它把安全执行、资源消耗、托管服务、AI 生态迁移和企业安全合规连接起来；但云厂商可能支持/托管/复制 E2B-like API，不等于无条件依赖 E2B 公司本身。
- 2026-07-01 已新增 `internship-reports/day33-e2b-protocol-and-agent-era-docker-study.md`：基于本地 clone `/tmp/e2b-research` @ `2b7dd17` 读取 E2B 官方 README、`spec/openapi.yml`、`spec/envd/process/process.proto`、`spec/envd/filesystem/filesystem.proto` 和 Python SDK，明确所谓 E2B protocol 不是 OCI/MCP 这类中立标准，而是 SDK + REST lifecycle + envd RPC + template/snapshot/network/volume 产品面的事实兼容对象。核心结论：E2B 像“Agent 时代 Docker”是因为它试图占住 agent sandbox 的默认开发者抽象层，类似 Docker 当年把 image/container/exec/files/network/volume/logs 打包成默认入口；AgentCube 当前不能声称 E2B-compatible，后续应单独做 facade proposal 和 official SDK conformance test。
- 2026-07-02 对比 Karmada push CI：Karmada `.github/workflows/ci.yml` 原生包含 `push`，注释明确 fork push 可帮助贡献者发 PR 前预检；AgentCube 核心 CI 仍以 `pull_request` 为主。已新增 `.agents/skills/agentcube-pr-management/scripts/check_push_ci.py`，并更新 PR skill：fork push CI 的关键不是查询脚本，而是分支里必须存在 push-triggered workflow。技术上把 workflow 加到个人 fork 默认分支后，后续包含该 workflow 的分支 push 会触发；但本仓库 `origin/main` 必须保持 `upstream/main` 干净镜像，不建议用于私有 CI 配置。若上游化，可考虑单独 PR 增加 `Branch Push Validation` workflow，定位为 contributor experience，不改 release/publish workflow，不默认把重型 e2e 强塞到每次 push；任何 upstream PR 仍需用户确认 exact diff/body。
- 2026-06-29 已新增 `internship-reports/week3-summary.md`：把 Day24-Day32 的 Sleep/Resume 控制面设计、Store CAS / Router activation / RuntimeProvider 边界、Agent Substrate 竞品复核、AgentCube 架构图和 Day29-Day31 的 #403/#387/#400 review 收束为 Week3 能力复盘；同时更新 `internship-reports/README.md`、根 `README.md` 和 `internship-reports/todo.md` 入口。
- 2026-06-29 已按用户反馈补齐 `internship-reports/day32-substrate-competitive-analysis-and-agentcube-prd.md` 遗漏的 Agent Substrate Counter Actor 架构图：输入材料表新增 `day28-agent-substrate-counter-architecture.png` / drawio / 高清 PNG，图解总览新增 Substrate 参考架构图和简短链路注释，用于支撑后续 Substrate -> AgentCube PRD 对比。
- 2026-06-28 按 mentor 反馈修正 CI 验证规则：以后不再通过向个人 fork 仓库提 PR 来跑 CI，因为这会制造 self-PR 历史并影响贡献质量信号。已更新 `AGENTS.md`、`.agents/skills/agentcube-pr-management/SKILL.md`、根 `README.md` 和 `internship-reports/open-source-contribution-format-standard.md`。当前 AgentCube 主要 CI workflow 是 `pull_request` 触发且 base 限定 `main` / `release-*`，普通 `feat/*` 分支 push 通常不会触发完整 CI；已新增 fork-only push CI 模板 `.agents/skills/agentcube-pr-management/templates/fork-push-validation.yml` 和安装脚本 `scripts/install_fork_push_ci.sh`。需要提前验证时，从真实 topic branch 切 `ci/<topic>` 验证分支，临时加入 `.github/workflows/fork-push-validation.yml` 并 push 跑 checks；最终 upstream PR 分支必须保持干净，不能包含该 workflow commit。
- 2026-06-26 已新增 `internship-reports/day32-substrate-competitive-analysis-and-agentcube-prd.md`，综合 Day28、`design.md` 和 `day28-agentcube-session-runtime-architecture-breakdown.md`，形成 Substrate 竞品分析、缺陷升级分析和 AgentCube Session Runtime Control Plane PRD。核心结论：AgentCube 不应复制 Substrate 的 gVisor/micro-VM 实现，而应把 Kubernetes-native agent-sandbox 基础升级为 Session lifecycle contract、Router resume-before-proxy、CAS-backed Store/Placement、RuntimeProvider、preservation level、benchmark/conformance suite 和可选 MultiAgent Worker / AgentSlot 高密度方向；开源打法是小 PR、强测试、清晰非目标和公开 benchmark，而不是一次性大重构。`internship-reports/todo.md` 已新增 Day32 已完成里程碑。用户反馈 Day32 文字过多后，已在文档前部补 `图解总览`：嵌入高清架构图，并新增 Substrate 机会转化、缺陷升级、PRD 闭环架构、开源贡献路线四张 Mermaid 图。
- 2026-06-26 已根据 `internship-reports/design.md` 生成 AgentCube 会话运行时架构图 `internship-reports/day28-agentcube-session-runtime-architecture.drawio`，并在 `design.md` 顶部加入图示链接。图把长文压缩为 Router activation gate、Session lifecycle workflow、CAS-backed Store/Placement、RuntimeProvider abstraction、Kubernetes capacity pool 五条主线；线色语义已补图例：蓝色请求/控制调用，紫色状态读写/CAS/Placement，绿色数据面 proxy/runtime endpoint，灰色虚线容量/健康/capability 信号。结构校验通过 `python .agents/skills/drawio-skill/scripts/validate.py ...`，无 error。已用本机 draw.io Desktop CLI `30.2.6` 导出预览 PNG `internship-reports/day28-agentcube-session-runtime-architecture.png`（2400x1619）和嵌入源图的高清 PNG `internship-reports/day28-agentcube-session-runtime-architecture.drawio.png`（3209x2165），并保留 diagrams.net 编辑快捷方式 `internship-reports/day28-agentcube-session-runtime-architecture.drawio.url`。`internship-reports/day28-agent-substrate-architecture-and-agentcube-differentiation.md` 已补充该架构图、PNG 和拆解文档链接，明确 Day28 已从 Substrate 分析推进到 AgentCube 自身架构设计产出。
- 2026-06-26 已新增 `internship-reports/day28-agentcube-session-runtime-architecture-breakdown.md` 作为架构图配套拆解文档，并从 `design.md` 顶部链接。文档以 Mermaid 为主、文字注释为辅，覆盖总览、连线语义、Ready 请求流、Paused 恢复流、Session 状态机、Pause/Resume CAS workflow、RuntimeProvider 抽象、Kubernetes capacity pool、控制面/状态面/数据面边界、失败补偿、设计优点和分阶段落地路线。
- Day31 已创建 `internship-reports/day31-picod-prometheus-metrics-review.md`，用于补今天实质工作量：从 upstream 候选中选择 PR #400 PicoD Prometheus metrics 做本地 review，而不是继续等待 #387/#403。已用 `agentcube-issue-discussion` / `agentcube-pr-management` 扫 #400/#405/#402/#394/#395 状态；#400 无 assignee、5 个文件、observability 主线最适合。隔离 worktree `/home/agentcube-pr400-review` 基于 PR head `3e4ac99`，验证通过 `go test ./pkg/picod -count=1`、`go test -race ./pkg/picod -count=1`、`go test ./pkg/picod -run TestMetrics_Exposition -count=20`。核心 finding：当前 `server.go` 中 `s.metrics.Middleware()` 注册在 `maxBodySizeMiddleware()` 后面，临时测试证明 oversized `POST /api/execute` 返回 413 但没有 `picod_http_requests_total{method="POST",path="/api/execute",status_code="413"}`；临时把 metrics middleware 移到 body-size limiter 前后同测通过，随后已删除临时测试并恢复 worktree clean。Day31 还记录了 `picod_active_executions` 统计整个 handler 而非仅 `cmd.Run()` 的指标语义问题、`/metrics` endpoint 不等同完整 Prometheus scrape 集成的 scope limitation、line-level review matrix、可选英文 upstream comment 草稿。用户已在 PR #400 发布评论 `https://github.com/volcano-sh/agentcube/pull/400#issuecomment-4808403017`，内容基于 Day31 复测证据；后续只跟踪作者/maintainer 回复，不主动继续评论。若 #400 作者 push 新 commit，先重新抓 head 并复测 413 metrics 行为。
- Day30 已创建并修正 `internship-reports/day30-pr387-warm-pool-dataflow-review.md`，专项 review PR #387 warm pool adoption 的实际运行数据流，而不是 API/import 适配。报告最前面已加入“结论先行”：#387 不是仅 e2e 黑盒通过，而是在 PR scope 内完成 AgentCube 对 `agent-sandbox v0.4.6` 的 API/dependency、controller object flow、claim adoption、runtime observation、Store/delete/cleanup 语义对齐。重要纠偏：不要拿 no-warm-pool direct Sandbox 图和 warm-pool adoption 图做旧新对比，原实现也有 warm pool；正确对比是旧 warm-pool 观测假设 `SandboxWarmPool -> Pod` / 按 claim 名推断 runtime 对象，和 #387 目标 `SandboxWarmPool -> Sandbox -> Pod` + `SandboxClaim.status.sandbox.name` bridge。核心结论：session create 时 WorkloadManager 创建 `SandboxClaim`，agent-sandbox controller adopt warm Sandbox 并写 status；WorkloadManager 再用 `SandboxPodNameAnnotation` / ownerRef fallback 找 Pod，最后写 Store。Store 应保留 `Kind=SandboxClaim`、`Name=<claim name>` 供 delete/GC 删除 claim，同时 `SandboxID` 和 `EntryPoints` 来自 adopted Sandbox/Pod。Day30 顶部已补回两张 sequenceDiagram 竖线图：旧 warm-pool 观测假设 `SandboxWarmPool -> Pod` 与 v0.4.6 真实流转 `SandboxWarmPool -> Sandbox -> Pod -> SandboxClaim adoption`。Day30 还在临时 k3d `agentcube-flow-v046` 安装 `agent-sandbox-controller:v0.4.6` 手工跑通 L1 object-flow：初始 warm pool Ready=2，`SandboxClaim/pr387-claim-1.status.sandbox.name=pr387-flow2-q48z9`，adopted Sandbox UID / Pod UID 不变，Sandbox ownerRef 从 WarmPool 转 Claim，warm pool refill 回 Ready=2，删除 claim 后 adopted Sandbox/Pod 清理；raw evidence 在 `internship-reports/benchmarks/day30-pr387-warmpool-flow/`。第一次未注入 `PICOD_AUTH_PUBLIC_KEY` 的 PicoD 模板 CrashLoop 只作为 failed-template evidence。临时 namespace/cluster 已清理。未向 upstream 评论，未改 #387 分支。
- Day30 文档结构已在 `intern` 复核清理：删除空的“一句话结论”层级，把目标/更正提前到 math-agent 示例前，把 PR 状态改为 2026-06-25 快照，并明确区分 L1 object-flow probe、PR-head live breakpoint test、CI/e2e 三层证据。math-agent 序列图已区分 Kubernetes Pod 对象与 Pod 内 PicoD，避免误解成 WorkloadManager 直接调用 PicoD。
- Day29 agentd cleanup 已进入 upstream review：用户确认后创建 `https://github.com/volcano-sh/agentcube/pull/403`，title `cleanup: remove unused agentd component`，head `ranxi2001:cleanup/remove-unused-agentd` @ `314e138`，base `volcano-sh:main`。该 commit 删除 `cmd/agentd` / `pkg/agentd`，移除 `build-agentd`，同步 docs/e2e wording，并修掉 `Picod (Agent Daemon)` 语义残留。Gemini 指出 docs 示例从 `/root/picod` copy 不够标准；用户指出原 `agentd` 示例使用 `/usr/local/bin/agentd`，因此已把 `docker/Dockerfile.picod` 的 binary 安装路径和 ENTRYPOINT 对齐到 `/usr/local/bin/picod`，文档示例同步使用 `/usr/local/bin/picod`。Day29 已补 scope 取舍复盘和 PicoD 源码速读：PicoD 是 sandbox 内 HTTP data-plane daemon，处理 JWT-protected `/api/execute` 和 `/api/files`，不同于旧 `agentd` controller。当前查询 PR #403 checks failed=0/pending=0，等待 reviewer/OWNER/tide；不要主动再 push/comment，若 reviewer 质疑 Dockerfile scope，可解释其服务于文档示例可执行性，或拆 follow-up。本地 Docker build 曾因拉 `golang:1.26.4` layer 卡住被手动中止，GitHub build check 已覆盖。
- Day28 已按用户要求重新 clone Agent Substrate 源码复核：`/tmp/agent-substrate` 当前 HEAD 为 `4bbd39f322c6`，原四面架构、router resume-before-proxy、Store CAS、Worker 同时最多承载一个 Actor 等判断仍成立。需要修正的口径是：当前 Substrate 不再只是 gVisor 路线，已有 `sandboxClass=gvisor|microvm`、`SandboxConfig` 和 `cmd/ateom-microvm`；Claude Code multiplex 是多个 Actor 在少量 Worker Pods 上按时间复用，不是一个 Worker Pod 内并发多 AgentSlot。已更新 Day28 报告、`day28-agent-substrate-counter-architecture.drawio` 和 explainer，把图明确限定为 gVisor counter 路径，并补 micro-VM/rootfs 语义 caveat。
- Day28 counter drawio 图已按用户反馈做局部 label 位置优化：保留线上的文字，不改成编号旁注；给边线 label 加白底和固定小字号，并用 edge geometry offset 移动到附近空白区域，重点修正 `DNS lookup`、`ResumeActor`、`Run / Restore / Checkpoint`、`informers / listers`、`forward` 和 `read logs` 等重叠点。已重新导出 `day28-agent-substrate-counter-architecture.drawio.png`，并把“复杂图导出后必须局部检视 edge-label overlap”的规则写入本地 `drawio-skill`。
- `internship-reports/design.md` 已从早期“placeholder Pod + 二级 sandbox scheduler”草稿重写为 Substrate-informed AgentCube 设计：保留 Kubernetes resource pool，但把主线升级为 Session lifecycle control plane、Store CAS / placement version、Router resume-before-proxy、RuntimeProvider / RuntimeClassConfig、preservation level 和可选 MultiAgent Worker Pod / AgentSlot。重点体现 Substrate 可学习点：低频 CRD + 高频 Store、Router activation gate、workflow 可重入、SandboxConfig/sandboxClass、worker cache；也明确不照搬一 worker 一 actor、gVisor/micro-VM checkpoint 绑定和早期 API 命名。
- 已给 `Agents365-ai/drawio-skill` 创建外部 PR：`https://github.com/Agents365-ai/drawio-skill/pull/49`，title `docs: add guidance for edge label overlap checks`，head `ranxi2001:docs/edge-label-overlap-check`，commit `a6b1132 docs: add edge label overlap guidance`，只改 `skills/drawio-skill/SKILL.md` 两处，新增复杂架构图 edge label 白底/offset 规则和 self-check 表项。已补 before/after 直观对比评论：`https://github.com/Agents365-ai/drawio-skill/pull/49#issuecomment-4808944533`。PR 当前 open、非 draft、mergeable clean。用户曾在聊天中明文提供 GitHub token，已按要求保存到 git-ignored `.agents/.env` 且权限 `600`；完成后建议用户在 GitHub 中 revoke/rotate 该 token。
- 已安装本地技能 `drawio-skill` 到 `.agents/skills/drawio-skill`，来源 `https://github.com/Agents365-ai/drawio-skill.git` 的 `skills/drawio-skill` 子目录。安装注意：系统默认 `python3` 是 3.6.8，官方 installer 需要用 `/root/.local/bin/python3.11`；仓库根目录没有 `SKILL.md`，正确 skill path 是 `skills/drawio-skill`。重启 Codex 后才能在可用 skills 列表中自动出现。
- 根 [README.md](README.md) 已在 `intern` 分支改成中文实习研发导航页，不再作为上游项目介绍使用；现在主要跳转 `internship-reports/todo.md`、`PROGRESS.md`、实习报告目录、Sleep/Resume、#387、OpenSandbox/Agent Substrate、Agent Infra 和协作纪律。原项目介绍仍保留入口到 `README-ZH.md` 和 upstream 仓库。
- Day24 已补 3A Store / GC split 完成记录：基于当前 `pkg/workloadmanager/garbage_collection.go`、`pkg/store/interface.go`、`pkg/common/types/sandbox.go` 和现有 GC tests 梳理源码基线，明确当前主线仍是 idle/TTL 直接 delete；3A 完成范围是设计落点、GC decision table、Store/lifecycle 边界、fake-lifecycle 单测骨架和 review checklist，不代表真实 Sleep/Resume 产品实现已合入。`internship-reports/todo.md` 中 3A 已标 DONE，真实代码仍等 #387 合并和 #386 分工明确。
- `internship-reports/todo.md` 已补 Sleep/Resume Stage 3 子任务拆解：3A Store/GC split、3B Router resume-before-proxy、3C WorkloadManager resume API、3D agent-sandbox hard-pause provider、3E e2e/math-agent validation。每项都标了是否适合我们做、是否等 #387 合并、是否会和 FAUST-BENCHOU 冲突、需要的 unit/e2e、产出类型和下一步。当前结论：#386 无正式 assignee 但 FAUST-BENCHOU 表示愿意接手；#387 仍 open，所以今天不发 upstream、不抢实现，优先设计/test skeleton/fake-provider validation。
- Day24 已补 2026-06-24 Stage 3 Sleep/Resume 设计推进：基于当前 `main` 源码重读 Router `handleInvoke` / `SessionManager`、WorkloadManager create/delete/GC、Store expiry/last-activity index 和 `SandboxInfo.Status`，明确下一步拆成 3A GC split 与 3B Router resume-before-proxy。关键结论：Router 必须 owner check 后再 resume，resume 成功后重新读 Store 使用新 entrypoint；GC 判断顺序应是 `maxSessionDuration` 优先、`ready` idle -> pause、`paused` pause timeout -> delete、transition 卡死可诊断；legacy/unknown status 不能直接 destructive delete。Day18 也补充了 `agent-sandbox v0.5.x` `OperatingMode=Suspended` 只是未来 hard-pause provider 信号，不等于已验证 Sleep/Resume、soft pause 或 checkpoint restore。
- Fork-only `intern` workflow preference已更新：实习报告、TODO、本地 skills 等记录类 commit 完成后，默认在同一轮直接 push 到 `origin intern:intern`；上游 PR、issue、评论、review request、maintainer mention 和官方 topic branch 仍需用户明确确认。该规则已写入 `AGENTS.md`。
- 已把 OpenSandbox issue [#800](https://github.com/opensandbox-group/OpenSandbox/issues/800) 补入 Day21 E2B 专题：该 issue 直接询问是否兼容 E2B 接口以方便迁移，维护者回复后关闭为 not planned。理由包括：E2B API 是具体 SaaS 产品接口而非开放标准；OpenSandbox 更希望维护自身 `specs/` 下 lifecycle / execd / egress API；完整兼容会引入 template/envd/process/PTY/beta/deprecated 能力、错误模型、鉴权、timeout、网络策略、文件操作、端口暴露等长期维护成本；更适合做迁移文档、对照表、示例或可选 e2b-style wrapper。这强化了 Day21 结论：OpenSandbox 是 E2B-like 自有 SDK，不是 E2B drop-in。
- Day21 已补 E2B 兼容性专题：明确 E2B 更适合被理解成事实上的 sandbox API/SDK 生态兼容对象，不是 OCI/MCP/Kubernetes CRD 这类中立标准协议。新增兼容等级 L0-L4：概念相似、SDK surface 相似、E2B API/SDK 部分兼容、drop-in 兼容。结论：CubeSandbox 明确做 E2B-compatible API server，是当前材料里的 baseline；OpenSandbox 是 `opensandbox.Sandbox.create` / `commands.run` / `files.*` 的 E2B-like 自有 SDK，不是 confirmed E2B drop-in；AgentCube 目前是 CodeInterpreter 自有 SDK/API，不能声称 E2B-compatible；Agent Substrate 是 actor substrate，不是 E2B API 实现。后续如做 AgentCube E2B 支持，应单独设计 facade，把 E2B sandbox ID 映射到 AgentCube session ID，并用官方 E2B SDK 跑 create/run/files/connect/kill conformance。
- Day27 已创建 `internship-reports/day27-agent-infra-career-roadmap-and-internship-goals.md`：把 Agent Infra 定义为让 LLM agent 安全、可靠、低延迟、可观测、可审计、可评估、可协作运行的基础设施，并按 runtime、Kubernetes/control plane、distributed session lifecycle、tool protocol/SDK、Router/API、安全治理、observability/eval、open-source review 八条能力线拆解。文档把官方资料（Kubernetes controller、CNCF cloud native、OCI runtime、MCP、OpenAI Agents SDK、OpenTelemetry、OWASP LLM Top 10、NIST AI RMF GenAI Profile）和本地 Day16-26 实践串联起来，形成职业岗位地图、能力等级、当前短板、后续 4 周目标和每周目标管理看板。`internship-reports/todo.md` 已新增 Day27 里程碑。
- Day26 末尾已补 upstream-facing enhancement issue proposal 草稿代码块：主题是把 AgentCube session lifecycle 拆成上层 Session/API/control-plane contract 与下层 Runtime/provider/substrate capability，两层分别覆盖 SDK ttl/delete/stop、Router match/resume-before-proxy、Store state/CAS/index、GC timeout、RuntimeProvider、agent-sandbox compatibility、NetworkPolicy/egress、benchmark evidence；草稿仅本地保存，未发布社区。
- Day26 已创建 `internship-reports/day26-week3-community-latest-and-two-layer-architecture-bug-surface.md`：把 2026-06-24 upstream 最新 issue (#401/#397/#394/#395/#392/#388/#386) 与 Day22-25 的 OpenSandbox/Agent Substrate 实测、Day23 架构路线、Day24 Sleep/Resume 设计、Day25 review matrix 串成 Week 3 的“面级别”总结。核心框架：上层 session/API/control-plane contract（SDK ttl/delete、Router match/resume-before-proxy、Store state/CAS/index、GC timeout、owner/auth、observability）与下层 runtime/provider/substrate capability（agent-sandbox v0.4/v0.5、CRD/controller/runtime、NetworkPolicy/egress、snapshot/checkpoint、benchmark realism）解耦；当前适合做 review/test feedback，不抢已 `/assign` 的实现。
- 2026-06-24 扫描 upstream open issues：最新活跃 issue 是 #401（Codegen Check path filter false pass，关联 PR #399 只清 go.sum，还需 workflow path filter 修复）；#397 讨论 CodeInterpreter direct sandbox path 未把空 authMode 默认成 picod；#394 讨论 Python SDK ttl 被 API 忽略；#395 讨论 AgentRuntimeClient 能创建 session 但不能显式 delete；#392 是 GitHub workflows hardening umbrella，已有 #393/#396/#399 相关 PR。#397/#394/#395/#388 均已有作者 `/assign`，不要抢实现，适合做 review/test feedback。
- Day21 连续补充术语注释并新建 `internship-reports/intern-glossary.md`：Day21 在 “一句话结论” 和 OpenSandbox 官方定位能力表下新增注释，解释 `gVisor checkpoint/restore`、独立控制面、路由层、provider adapter、snapshot state、路由唤醒、egress/credential/audit、冷启动拆分、kind/GKE/state store/substrate、PTY、Ingress/egress、FQDN/CIDR、Credential Vault、secure endpoint、runc/gVisor/Kata/Firecracker 等；新术语文件按 L0-L4 分级整理 API/SDK/CLI、Kubernetes/control plane、sandbox runtime、lifecycle/state machine、network/security/benchmark 术语，并附常见对照关系。
- Day21 的同一 gRPC/protobuf 注释区已补 `proto` 基础解释：说明 `.proto` 是 Protocol Buffers 的接口定义文件，是人和机器都能使用的 API 契约，并用最小 `ResumeActorRequest` / `ResumeActorResponse` / `ActorService` 示例解释 `message`、`service`、`rpc` 和字段编号，帮助后续阅读 Agent Substrate 的 `ateapi.proto`。
- Day21 的 gRPC 概念注释已继续补充：在 `internship-reports/day21-opensandbox-agent-substrate-study.md` 开头资料来源后的 `gRPC API` 注释处加入“常见上层通信方式”表，只保留 REST/HTTP JSON、gRPC/protobuf、WebSocket、SSE、Message Queue/PubSub、Kubernetes API/CRD，去掉 TCP/UDP/QUIC 等底层传输协议，方便后续阅读 OpenSandbox / Agent Substrate / AgentCube 文档时快速区分外部入口、内部控制面、状态流、异步任务和声明式资源。
- `AGENTS.md` 已新增文档写作规范：实习报告、设计笔记、架构调研、review-prep 文档中不要吝啬解释性注释；遇到抽象概念、跨项目术语、协议、Kubernetes 资源、状态机、控制面/数据面、安全术语或工程推断时，应在相关段落/表格附近加 `> 注释：...` / `> 分析：...`，方便未来 reviewer 脱离聊天上下文阅读。该规则仅针对学习/报告文档，代码注释仍保持精确和必要。
- Day21 已按用户要求补充 gRPC / proto / 控制面概念注释：`internship-reports/day21-opensandbox-agent-substrate-study.md` 新增多处 `> 注释` / `> 分析`，解释 gRPC 与 REST、CRD、Kubernetes reconcile、控制面/数据面、SessionIdentity、snapshot stream 的区别，并新增 “gRPC / proto 对 AgentCube 的启发” 小节。结论：AgentCube 当前不应为了协议先进性直接改 gRPC；更重要的是先把 session lifecycle contract、Router resume-before-proxy、Store 高频状态/CAS、runtime provider 边界定义清楚。Day21 当前 633 行，已超过同主题续写 600 行阈值。
- Day24 已补完整 `internship-reports/day24-sandbox-sleep-resume-design-note.md`：包含 Sleep/Resume 中文设计分析、英文 proposal 草稿、本地 spike 验收口径和 spike 结果。结论仍是 AgentCube 不等待 agent-sandbox pause 全部定稿，但也不重复底层 runtime pause；AgentCube 先定义 session lifecycle contract（Ready/Pausing/Paused/Resuming/Deleted/Failed）、Store CAS/状态和 pause indexes、Router resume-before-proxy、GC split、SDK ttl/delete 语义和 RuntimeProvider capability。隔离 worktree `/home/agentcube-sleep-resume-spike` branch `spike/sleep-resume-state-machine` 基于 `upstream/main bed6bd4` 新增本地实验包 `pkg/sessionlifecycle`，已验证 fake provider、Store CAS、GC decision 和并发 resume：`go test ./pkg/sessionlifecycle -count=1`、`go test -race ./pkg/sessionlifecycle -count=1`、`go test ./pkg/sessionlifecycle -run TestResumeSession_ConcurrentResumeUsesStoreCAS -race -count=20`、`git diff --check` 均通过。关键发现：Store CAS 是并发 resume 正确性要求，不是优化项；pause 后必须清空/标记旧 entrypoint，resume 后必须刷新 entrypoint。
- Day24 第一阶段实现已完成并写回报告：`/home/agentcube-sleep-resume-store-state` branch `feat/sleep-resume-store-state` 基于 `upstream/main bed6bd4` 实现 Store 状态/CAS 基础，不接真实 agent-sandbox。改动包括 `SandboxInfo` pause/resume 字段和状态常量、Store 接口新增 `UpdateSandboxStatusCAS` / `ListPauseExpiredSandboxes`、Redis/Valkey `session:pause_expiry` index、Lua CAS 脚本、Redis/Valkey 成功/冲突/not-found/并发 CAS/pause index/delete 清理测试。验证通过：`go test ./pkg/store -count=1`、`go test ./pkg/router ./pkg/workloadmanager -count=1`、`go test -race ./pkg/store -count=1`、`go test $(go list ./... | grep -v '/test/e2e') -count=1`、`make lint`、`make build-all`、`git diff --check`。`make build-all` 产生过无关 `go.sum` tidy 变化，已按最小修原则恢复。
- Day24 第二阶段实现已完成并写回报告：同一 worktree `/home/agentcube-sleep-resume-store-state` branch `feat/sleep-resume-store-state` 在第一阶段 commit `3d0427a` 之后新增 commit `cb66c8a feat: add workload manager session lifecycle service`。第二阶段只新增 `pkg/workloadmanager/session_lifecycle.go` 和 `pkg/workloadmanager/session_lifecycle_test.go`，实现 WorkloadManager 内部 `sessionLifecycleService`、`RuntimeProvider` capability 边界、`pauseSession` `ready -> pausing -> paused`、`resumeSession` `paused -> resuming -> ready`、provider failure -> `failed`、CAS conflict -> `ErrSessionStateConflict`。fake provider/in-memory store 测试覆盖 pause/resume success、pause/resume provider failure、concurrent resume CAS conflict、wrong initial state。验证通过：`go test ./pkg/workloadmanager -run 'TestSessionLifecycleService' -count=1`、`go test ./pkg/store ./pkg/workloadmanager -count=1`、`go test -race ./pkg/workloadmanager -run 'TestSessionLifecycleService' -count=1`、`make lint`、`go test $(go list ./... | grep -v '/test/e2e') -count=1`、`make build-all`、`git diff --check`。`make build-all` 仍会删除 `go.sum` 里两行 `golang.org/x/oauth2 v0.36.0` checksum，已按最小修原则恢复，最终代码分支 clean。
- Day25 已创建并扩写 `internship-reports/day25-sleep-resume-code-review-and-architecture-retrospective.md`，按 reviewer 视角复盘 `/home/agentcube-sleep-resume-store-state` 两层 commit，当前已超过 600 行。结论：Store/CAS 和 WorkloadManager lifecycle service 的阶段拆分合理，但还不是完整 Sleep/Resume；当前最大后续风险是 provider 成功后 final CAS 失败导致 runtime 与 Store 状态不一致，其次是 failure state 语义、一次性新增字段较多、`RuntimeCapabilities` 尚未作为 gate 使用。Day25 已形成 code review matrix、风险分级、风险到测试矩阵、GC decision table、Router status handling、错误/retry 语义、RuntimeProvider gate、测试设计、PR 拆分建议和 mentor 汇报口径。规则更新：同一主题日报/复盘至少写到 600 行再考虑换新文件。
- Week2 文档已从计划改为总结：`internship-reports/week2-work-plan.md` 重命名为 `internship-reports/week2-summary.md`，主题改成“从写代码转向审代码与工程判断”。文档已沉淀 mentor 视角：可以假设以后不亲手写代码，而是重点训练需求拆分、设计文档、架构边界、代码目录落地、代码审查、分层测试、CI/CD 和开源协作规范。README/TODO 链接已更新；`AGENTS.md` 已新增规则：周总结重点记录可复用工程判断和 review 能力，不只统计代码产出。
- 本机 Go 环境已修复：Go `1.26.4` toolchain 已安装到 `/usr/local/go1.26.4`，`/usr/local/bin/go` 和 `/usr/local/bin/gofmt` 已链接到该版本。现在 plain `go version` 输出 `go version go1.26.4 linux/amd64`，plain `make fmt` 可直接运行；无需再给每条命令手动加 `/root/go/pkg/mod/golang.org/toolchain@v0.0.1-go1.26.4.linux-amd64/bin`。
- Day23 已创建 `internship-reports/day23-agentcube-future-architecture-and-design.md`，基于 `design.md`、官方 README/architecture/proposal、最新 PR/issue（#386/#387/#400/#367/#398/#394/#395/#331/#291/#382/#301/#354/#366/#379）和 Day12/15/18/19/20/21/22 记录，形成 AgentCube 未来架构路线：继续调研但从泛读转为设计/测试驱动；近期重点不是造新 runtime 或二级 scheduler，而是把 AgentCube 做成稳定的 session lifecycle control plane。核心路线为 `agent-sandbox v0.4.6` compatibility -> RuntimeProvider capability layer -> Store session state/index -> Router resume-before-proxy -> GC Ready/Paused/Deleted split -> Sleep/Resume MVP -> benchmark/math-agent validation -> SnapStart/runtime acceleration -> 可选 SandboxWorkspace/resource pool。
- Day21 已按 Day22 端到端实测回灌更新：`internship-reports/day21-opensandbox-agent-substrate-study.md` 新增 2026-06-23 更新说明、OpenSandbox Docker runtime 实测后的理解修正、Agent Substrate kind quickstart bootstrap 失败分析、三方源码级对照表的“实测状态”行、更新后的尖锐结论、测试纪律（冷/热启动分开、readiness 独立测、清理状态作为通过条件）和新的下一步。核心变化：OpenSandbox 不只是文档完整，Docker CLI/SDK/execd/file API 已本机跑通；Agent Substrate 仍是 sleep/resume 架构样本，但部署 substrate 本身是第一风险，当前机器未进入 counter demo。
- Day22 端到端实测已执行并写回 `internship-reports/day22-opensandbox-agent-substrate-runtime-runbook.md`，原始日志在 `internship-reports/benchmarks/day22-opensandbox-substrate-smoke/`。OpenSandbox Docker runtime 通过 CLI 与 Python SDK 两条 e2e：health、create `python:3.12` sandbox、command 输出 `2`、file write/read、kill、list empty、Docker 无残留。首次 create 因 server 同步拉 `python:3.12` 镜像耗时约 246s，CLI `httpx.ReadTimeout` 且 `/health` 期间也会超时；预拉 `opensandbox/execd:v1.0.19` 和 `python:3.12` 后通过。SDK 脚本需读 `execution.logs.stdout[0].text`，不是 `result.stdout`。Agent Substrate 用临时 Go PATH 绕过 `go` 不在 PATH 后，`hack/create-kind-cluster.sh` 失败在 kind/kubeadm control-plane 初始化：cgroup v1 warning，创建 ClusterRoleBinding 请求持续无响应，最终 `client rate limiter Wait returned an error: context deadline exceeded`；未进入 Agent Substrate system/counter demo。已清理：无 kind cluster、无 Docker 容器、8080/5001 释放。
- Day22 已创建 `internship-reports/day22-opensandbox-agent-substrate-runtime-runbook.md`，把 Day21 的 OpenSandbox / Agent Substrate 后续实测拆成分层 runbook：OpenSandbox 先跑 Docker server health / create python sandbox / command / file read-write / delete，再进入 Kubernetes BatchSandbox 和 agent-sandbox provider；Agent Substrate 先跑 kind quickstart + counter demo，验证 CreateActor -> router request -> ResumeActor -> SuspendActor -> 再次 resume 后计数保留。当前本机前置检查显示 Docker 26.1.3、uvx 0.11.20、kubectl client v1.24.17+k3s1、kind v0.32.0 可用，但系统 Python 为 3.6.8，OpenSandbox 需用 uvx/uv 管理 Python 3.10+；当前 shell 里 `go` 不在 PATH，Agent Substrate quickstart 会先卡在 Go toolchain。
- Day21 已完成 OpenSandbox / Agent Substrate 二次源码深读：`internship-reports/day21-opensandbox-agent-substrate-study.md` 已补 OpenSandbox provider 源码、Agent Substrate actor/resume/pause 工作流、AgentCube Router/WorkloadManager/store 对照，并新增 AgentCube / OpenSandbox / Agent Substrate 三方源码级表格和尖锐结论；三处简化调用链/架构图已改为 Mermaid。核心判断：OpenSandbox 的 adapter 边界更清楚；Agent Substrate 更接近 AgentCube sleep/resume 未来态；AgentCube 当前 main 仍是 create/delete/warm pool，未形成 Paused/Resume 闭环。
- Day15 报告已补入 2026-06-17 14:30 线上会议任务：和 `FAUST-BENCHOU` 讨论 AgentCube 下一步计划，重点围绕 v0.2.0 umbrella issue #386。
- `internship-reports/todo.md` 已新增 P0 任务“讨论 AgentCube v0.2.0 下一步计划”，下一步是整理 #386 中文内部总结并明确会后是否发英文 proposal / review / test plan。
- 已核查 #386 中 `zhzhuang-zju` 的 agent-sandbox 适配提案：AgentCube 确实依赖 `sigs.k8s.io/agent-sandbox v0.1.1`；临时升级到 `v0.4.6` 会因 `controllers.SandboxPodNameAnnotation` 缺失编译失败，但 `SandboxWarmPool` / `SandboxClaim` 类型和核心字段仍存在。
- 已溯源 agent-sandbox 编译失败：`v0.2.1` 可编译，`v0.3.10+` 失败；直接原因是 `SandboxPodNameAnnotation` 从 `controllers` 包迁到公开 API 包 `api/v1alpha1`，深层背景是 warm pool 从 bare Pod adoption 重构为 full Sandbox CR adoption。
- Day15 报告已更新更强版 #386 英文 comment 草稿：最小常量修复 + 依赖升级可让 `v0.3.10`/`v0.4.6` 非 e2e 测试通过，但不保证运行；新版 warm pool 可能 adopt generated-name Sandbox，AgentCube 当前按 claim/sandboxName watch Ready 事件，存在漏通知/超时风险。
- #386 评论已发布：https://github.com/volcano-sh/agentcube/issues/386#issuecomment-4725375092；当前暂无 maintainer 回复。
- 已固化“issue/PR 分析优先脚本化”的偏好：`AGENTS.md`、`agentcube-issue-discussion`、`agentcube-pr-management` 都已加入脚本优先和持续优化 skill 的规则。
- 新增 `.agents/skills/agentcube-issue-discussion/scripts/thread_brief.py`，用于输出 compact Markdown 版 issue/PR 摘要；已用 #386 和 #385 验证，能捕捉 PR review 中的 `/assign` 信号。
- 已对 #386 中 FAUST-BENCHOU 的 Sandbox Sleep/Resume 提案做代码级分析并写入 Day15：当前实现是 idle/TTL 直接删除式 GC，不是 Ready -> Paused -> Ready；最小实现也要跨 API/CRD、store state、GC、WorkloadManager pause/resume、Router resume-before-proxy、agent-sandbox replicas=0/1 语义和 e2e。
- 已补充 #386 两个提案关系：agent-sandbox 适配是 Sleep/Resume 的底层兼容性 foundation；如果先按 v0.1.1 实现 Sleep/Resume，后续升级到 v0.3.10+/v0.4.6 可能因 SandboxClaim / warm pool / Ready condition 语义变化返工。
- 已将 `/home/agentcube/会议记录` 中 2026-06-17 AgentCube 例会整理进 Day15：会后方向扩展为 agent-sandbox compatibility、Sleep/Resume lifecycle、E2B-compatible API/SDK/template、SnapStart/MicroVM snapshot/runtime 能力四层关系。
- Day16 已创建 `internship-reports/day16-agent-sandbox-latest-adaptation.md`，记录 `agent-sandbox` 适配任务由 ranxi 独立负责，并按“两条腿走路”拆成升级历史/行为审计与直接升级/测试驱动适配两条路线。
- 已创建 clean code worktree `/home/agentcube-agent-sandbox-latest`，分支 `feat/agent-sandbox-latest`，基线 `upstream/main` commit `0fd9151`；主目录继续保留实习报告，不把中文记录混入 upstream PR 分支。
- 在 feature 分支中 `go get sigs.k8s.io/agent-sandbox@v0.4.6` 已复现：该版本要求 Go `>=1.26.2`，并带动 K8s/controller-runtime 等依赖升级；随后 `go test ./pkg/workloadmanager ./cmd/workload-manager ./cmd/agentd` 失败在 `controllers.SandboxPodNameAnnotation` 未定义。
- 已确认 `v0.5.0rc1` Git tag 存在且可由 Go 解析为 pseudo-version `v0.4.7-0.20260608211546-6af1bbd0cf64`，但 `go list -m ...@latest` 仍是 `v0.4.6`，PR 是否依赖 rc/pseudo-version 需维护者确认。
- `/home/agentcube-agent-sandbox-latest` Day16 适配已推进到真实运行验证：升级 `agent-sandbox v0.4.6`，移除 `controllers` 内部包依赖，SandboxClaim 路径等待 `claim.Status.SandboxStatus.Name` 指向的 adopted Sandbox，store 仍保存 claim 名用于 delete/GC；另发现并修复 v0.4.6 `SandboxTemplate` 默认 Managed NetworkPolicy 阻断 AgentCube Router/WorkloadManager 的问题，当前显式设 `networkPolicyManagement: Unmanaged`。
- Day16 验证已通过：`go test ./pkg/workloadmanager`、`go test -race ./pkg/workloadmanager`、非 e2e Go 全量、`make build-all`、Docker build 三镜像、Helm template/lint、k3s direct CodeInterpreter e2e、warm pool e2e、warm pool load 100/100、Python SDK、LangChain sandbox、MCP HTTP/stdio、math-agent LLM e2e（OpenAI-compatible base URL 需带 `/v1`）。
- 新增 `.agents/skills/llm-e2e-test/SKILL.md`，用于复用 AgentCube math-agent / OpenAI-compatible LLM 端到端测试流程；已通过 `quick_validate.py`。
- Day16 `agent-sandbox v0.4.6` 适配代码已提交并 push：`/home/agentcube-agent-sandbox-latest` branch `feat/agent-sandbox-latest` commit `5316358`，DCO signoff 已包含，origin 分支已存在。Upstream PR 已创建：#387 `https://github.com/volcano-sh/agentcube/pull/387`。第一次 token API 创建返回 `401 Bad credentials`；第二次 token 创建成功。token 未落盘，临时 body 已删除，workspace 文件中未发现 GitHub token pattern；用户应 revoke / rotate 明文暴露过的 token。
- PR #387 初始状态已更新：open、非 draft、labels `kind/bug` / `size/XL`，commit `5316358`。当前 DCO、build、e2e-test、Codegen Check、python-sdk-tests、Python Lint、codespell 已通过；失败 check 为 `coverage` 和 `golangci-lint`；bot 提示 review 获得 `lgtm` 后再 assign `hzxuzhonghu` approval。
- 用户要求后续 GitHub API/PR 操作不再重复索要 token；本地 token 已保存到 git-ignored `.agents/.env`，变量名 `GITHUB_TOKEN` / `GH_TOKEN`，权限 `600`。只记录位置和变量名，不记录密钥值。
- Day16 提交前新发现并修复 codegen 问题：旧 `hack/update-codegen.sh` 固定 `code-generator v0.34.1` 且用 `go get -d`，会在生成过程中把 `agent-sandbox` 降级；当前 PR 已对齐 `code-generator v0.35.4` 并改用 `go mod download`。commit 后 `make gen-check` 已通过。
- Day17 已创建 `internship-reports/day17-pr387-copilot-review-and-ci-triage.md`：Copilot 7 条评论合并为 4 类（direct wait nil channel guard、annotation comment、e2e warm-pool UID 匹配、recordingStore 错误传播和 slice deep copy）；`golangci-lint` 失败根因初判为 workflow 用 Go 1.24 构建 linter，而 PR `go.mod` target 为 Go 1.26.2；`coverage` 失败发生在 `go test -race -coverprofile=coverage.out -coverpkg=./pkg/... ./pkg/...`，日志可见 package summary 均为 `ok`，需要 exact command 复现。
- Mentor 建议已固化到 `.agents/skills/agentcube-pr-management/SKILL.md` 和 Day17：不要默认在已打开的 upstream PR 上不断累积 review/CI 修复；先用临时 fix branch 或独立小 PR 验证，属于原 PR 范围的再 clean rebase/squash/cherry-pick 回原 PR，通用 CI/toolchain 修复应从 `upstream/main` 单独提 PR，合并后再让原 PR rebase。
- `/home/agentcube-agent-sandbox-latest` 已从 #387 head 创建验证分支 `fix/pr387-review-feedback`，commit `13f19e3 fix: address agent-sandbox adaptation review feedback`，并推送 fork stacked PR `https://github.com/ranxi2001/agentcube/pull/1`（base `feat/agent-sandbox-latest`，head `fix/pr387-review-feedback`）。该 commit 修复 lint/coverage workflow Go 版本、Copilot 指出的 nil watcher guard、annotation comment、warm-pool e2e UID 匹配、recordingStore error/deep copy，并降低两个 gocyclo 报警点。
- 已按用户确认创建 upstream CI 验证 PR #390：`https://github.com/volcano-sh/agentcube/pull/390`，标题 `[DO NOT MERGE] validation for PR #387 with review fixes`，分支 `test/pr387-with-review-fixes` 基于 `upstream/main`，包含两个 commit：`2c93451`（#387 原始适配）和 `6f94052`（review/CI 修复）。该 PR 只用于跑 upstream bot/CI，不作为最终合并目标。#390 最新 CI 已全绿，包括 `coverage`、`golangci-lint` 和 `e2e-test`。
- #390 新增 AI review 已 triage 并写入 Day17：Gemini 关于 `client-go` fake client WatchList 方法名的 `critical` 评论是误报，`client-go@v0.35.4` 和 `code-generator@v0.35.4` 实际接口/生成器均使用 `IsWatchListSemanticsUnSupported()`；Copilot 的生成代码注释评论不应在 AgentCube PR 手改。有效评论是 3 条：`waitForDirectSandboxReady` 需要处理 closed channel / nil sandbox，`waitForClaimSandboxReady` 遇到 context error 应立即返回，`hack/update-codegen.sh` 的 `sed` 解析不应依赖尾随逗号。暂不回灌 #390 到 #387。
- #390 暴露的 upstream 协作流程问题已固化为规则：提交任何 upstream PR/draft/WIP、issue、comment、review comment、`/assign`、request review 或 mention 维护者之前必须先让用户确认完整内容；upstream PR 必须使用官方 `.github/PULL_REQUEST_TEMPLATE.md`，draft/WIP 也不能自拟风格；未完成 upstream PR 用 `[WIP]`，不用 `[DO NOT MERGE]`；只为跑 CI 时优先用 fork branch / fork PR / fork Actions / 本地测试；阅读分析别人的 PR 默认写本地报告，不需要维护者验证就不发社区。
- 已更正“不要在一个 PR 反复更新”的理解：这不是机械禁止更新 PR。当前 PR 引入的问题可以验证后 clean update 当前 PR；需要拆出去的是独立前置条件或仓库级兼容性变化。Go/toolchain 升级属于 `agent-sandbox` 适配的独立前置条件，正确路径是从 `upstream/main` 做纯净 Go/toolchain upgrade 分支，先证明原始 AgentCube 项目在新 Go 下 build/test/lint/e2e 能跑，再让 #387 rebase 到合入后的 `main`。
- 纯净 Go/toolchain 前置分支已按 mentor 建议重做：官方 Go feed 当前稳定版本为 `go1.26.4`，workflow 改用 `go-version-file: go.mod`，Docker builder 镜像显式对齐到 `1.26.4`。`/home/agentcube-go-toolchain-upgrade` branch `chore/go-stable-toolchain`，base `upstream/main 0fd9151`，commit `3f1a823 chore: update Go toolchain to 1.26.4`。本地验证通过：非 e2e Go tests、race coverage、`make build-all`、`make lint`、提交后 `make gen-check`、三 Docker builds、`git diff --check`。fork validation PR `https://github.com/ranxi2001/agentcube/pull/3` CI 全绿：codespell、Codegen Check、Python Lint、两个 build、coverage、e2e-test、golangci-lint、python-sdk-tests。旧 1.26.2 fork PR #2 已关闭为 superseded。
- upstream Go/toolchain PR #391 已合并：`https://github.com/volcano-sh/agentcube/pull/391`，title `chore: update Go toolchain to 1.26.4`，merge commit `a31651e5aba6ab0ce6ef854ffdb724146b40af5b`，merged at `2026-06-18T06:37:52Z` by `volcano-sh-bot` after `RainbowMango` approved with `/lgtm /approve` and “Good job! Thanks.” This is the accepted Go 1.26.4 prerequisite for #387.
- PR #385 已按 Gemini 建议更新：`WarmPoolNotFound` condition 保留，但不再记录 Warning Event。
- PR #385 最新 commit `d885b4e` 已 push，DCO 漏签已用 `git commit --amend --no-edit --signoff` 修复。
- PR #385 Gemini thread 已回复，附了 focused tests、`go test ./pkg/workloadmanager` 和 `git diff --check`。
- PR #379 已读实现范围：CRD、controller、agentd Kuasar driver、artifact store、CodeInterpreter restore intent。
- PR #379 找到一个非重复 review 点：promotion 重置 `ReadyAt` 后，可能因 `snapshotStatusEqual` 不比较 `ReadyAt` 而没有持久化，导致 `RebuildAfter` 继续触发 rebuild。
- #391 合并后已在 `/home/agentcube-agent-sandbox-latest` 创建本地验证分支 `rebase/pr387-on-go1264`：从 `origin/feat/agent-sandbox-latest` rebase 到 `upstream/main a31651e`，冲突仅为 Go/toolchain 文件并保留 Go `1.26.4`。直接 rebase 后 `go test ./pkg/workloadmanager` 和 coverage exact command 通过，但 `make lint` 仍失败，说明 #391 只解决工具链基线，不能自动修复 #387 自身代码问题。
- `rebase/pr387-on-go1264` 已补本地 commit `5867183 fix: address agent-sandbox adaptation review feedback`，处理 direct watcher nil/closed/nil sandbox、claim context error、lint complexity、recordingStore、e2e warm-pool UID matching、codegen sed 解析；验证通过：`go test ./pkg/workloadmanager -count=1`、`make lint`、`go test -race ./pkg/workloadmanager -count=1`、coverage exact command、非 e2e Go tests、`make gen-check`、`go test ./test/e2e -run '^$' -count=1`、`make build-all`、`git diff --check`、`git diff --exit-code`。该分支尚未 push，也未更新 #387。
- Fork CI validation PR 已创建并全绿：`https://github.com/ranxi2001/agentcube/pull/4`，base `release-pr387-go1264` -> `a31651e`，head `ci/pr387-rebase-go1264` -> `5867183`，mergeable clean。完整通过 Codespell、Python SDK Tests、Python Lint、Copyright Check、Codegen Check、Agentcube CI Workflow build jobs、Lint/golangci-lint、Test Coverage、Agentcube E2E Tests。注意：fork PR base 需用 `release-*` 或 `main` 才会触发完整 PR workflows；普通 `ci/*` base 只触发无 branch filter 的 coverage / python-sdk-tests。
- 用户确认后已更新 upstream PR #387：`ranxi2001:feat/agent-sandbox-latest` force-with-lease 从 `5316358` 更新到 `5867183`，PR body 已去掉旧 Go 1.26.2 / Go 版本前置问题描述，保留 dependency stack 说明和 fork CI evidence。Upstream checks 全绿：DCO、Approve workflows、codespell、Codegen、Python Lint、两个 build、coverage、e2e-test、golangci-lint、python-sdk-tests。当前 `mergeable_state=unstable` 预计是等待 maintainer review / lgtm / approve / tide，不是测试失败。
- 已按源码方式分析 #387 中 `zhzhuang-zju` 对 label 和 `v0.5.0rc1` 的评论。结论：#387 更适合 `/kind feature`；`v0.5.0rc1` 不是当前 Go module `@latest`，会解析为 pseudo-version，且源码已从 `api/v1alpha1` / `extensions/api/v1alpha1` 迁到 `v1beta1`。本地试验显示 `go mod tidy` 先因 v1alpha1 package 缺失失败；机械改 import 后又失败在 `SandboxSpec.Replicas` 被 `OperatingMode` 替换、`SandboxClaimSpec.TemplateRef` 被 required `WarmPoolRef` 替换。Day17 已记录完整源码证据和英文回复草稿。
- 新增 `.agents/skills/agentcube-pr-management/scripts/audit_go_module_version.py`，并补充 PR skill 的 Dependency Release / RC Triage 流程，用于复用 `go list @latest/@tag/-versions`、package presence 和最小 bump 实验。
- 用户确认后更新了 PR #387 body：`What type of PR is this?` 已从 `/kind bug` 改为 `/kind feature`，`What this PR does / why we need it` 已改为 current stable `agent-sandbox v0.4.6` compatibility feature 口径，target-version note 也说明 `v0.5.0rc1` 不只是 pseudo-version 问题，更涉及 `v1alpha1` -> `v1beta1`、`TemplateRef` -> required `WarmPoolRef`、`Replicas` -> `OperatingMode` 的语义迁移。随后按用户要求改 label；直接 Labels API 因无 admin 权限返回 403，已用 bot 命令评论 `/remove-kind bug` + `/kind feature` 完成，当前 labels 为 `kind/feature` / `size/XL`。未发布解释性技术回复。
- 已形成 `agent-sandbox v0.5.x` follow-up 计划并写入 Day17/TODO：#387 保持 current stable `v0.4.6` 兼容范围；`v0.5.x`/`v1beta1` 迁移另开干净 follow-up，核心改动包括 imports/scheme/GVR v1beta1、direct Sandbox `Replicas -> OperatingMode`、warm-pool claim `TemplateRef -> WarmPoolRef`、真实 v0.5.x manifests/e2e/math-agent 验证，并把 Sleep/Resume 作为后续状态机设计而非混入兼容 PR。
- 用户确认将 `agent-sandbox v0.5.x` 前沿适配测试拆出 #387；已新增 Day18 `internship-reports/day18-agent-sandbox-v05-forward-adaptation.md`，并把 TODO 拆成 stable `v0.4.6` review 任务和 `v0.5.x/v1beta1` fork-only validation 任务。
- Day18 `agent-sandbox v0.5.0rc1` 前沿适配已完成真实 runtime 和 fork CI 验证：`/home/agentcube-agent-sandbox-latest` fork branch `test/agent-sandbox-v05-forward` 当前 head `c0122da`，包含最小 v1beta1/API 迁移 commit `ee1aecf` 和 e2e manifest 版本对齐 commit `c0122da`。Fork PR `https://github.com/ranxi2001/agentcube/pull/5`，base `release-agent-sandbox-v05-base` -> `5867183`，head `test/agent-sandbox-v05-forward` -> `c0122da`，所有 checks 全绿：approve workflows、codespell、Codegen、Python Lint、两个 build、coverage、e2e-test、golangci-lint、python-sdk-tests。第一次 fork CI 失败根因是 e2e setup 仍安装旧 agent-sandbox manifest，集群无 v1beta1 CRD，workloadmanager 报 `no matches for kind "SandboxWarmPool" in version "extensions.agents.x-k8s.io/v1beta1"`；`c0122da` 已将 `make e2e` / `run_e2e.sh` 默认 agent-sandbox manifest 对齐到 `v0.5.0rc1`。未更新 #387，未创建 upstream PR。
- Day18 runtime 关键发现：当前默认 k3s 的 agent-sandbox CRD 仍是 `v1alpha1` storedVersions；rc1 manifest 是 v1beta1-only，`kubectl apply --dry-run=server` 会因 `status.storedVersions[0]: "v1alpha1" must appear in spec.versions` 拒绝原地 apply。干净 k3d 集群可直接安装 rc1。后续不能把“代码能跑”说成“现有集群可无缝升级”，需要单独处理 CRD migration / clean install 口径。
- 已更新 PR skill / AGENTS：完整 GitHub Actions CI 通常由 `pull_request` 事件触发，单纯 push fork branch 不会跑全量 checks；需要 fork CI 时创建 fork PR，base 用 `main` 或 `release-*` 以匹配 workflow branch filters。
- 已完成 #387 下周 review 准备材料：[Day19](internship-reports/day19-pr387-code-review-prep.md) 按文件/行级别整理了 changed files 的修改原因、为什么这样改、local-vs-project 分类、`go.mod` 依赖栈解释、generated files 口径、review Q&A 和测试覆盖矩阵。重点结论：#387 是 `agent-sandbox v0.4.6` current stable compatibility feature；`k8s.io/* v0.35.4` 由 `agent-sandbox v0.4.6` 依赖链驱动；claim path 必须通过 `SandboxClaim.Status.SandboxStatus.Name` 找 adopted Sandbox，但 store 仍保存 claim 名；`NetworkPolicyManagementUnmanaged` 是保持现有 AgentCube connectivity。`pkg/workloadmanager/k8s_client.go` 段落已扩展，解释了 `getSandboxClaim -> getSandbox` 读取链路、dynamic client 选择、unstructured/typed conversion 和 review 答辩口径。`pkg/workloadmanager/sandbox_helper.go` 段落已补充 placeholder `CreatedAt` / `ExpiresAt` 和 store expiry index 关系。`test/e2e/e2e_test.go` 段落已补充删改较多的原因：三处旧 warm-pool bare Pod 过滤逻辑统一到 `listWarmPoolPods`，以兼容旧 `SandboxWarmPool -> Pod` 和新版 `SandboxWarmPool -> Sandbox -> Pod` ownerRef 链。
- 用户确认后已 push #387 最小修复 commit `ff8260e chore: drop unrelated picod Dockerfile cleanup` 到 `origin/feat/agent-sandbox-latest`。PR #387 当前 changed files 为 15，`docker/Dockerfile.picod` 已不在 diff 中。验证已通过：`go test ./pkg/workloadmanager -count=1`、`docker build -f docker/Dockerfile.picod -t agentcube-pr387-minimal-picod:test .`、`make build-all`、`make gen-check`、`go test ./test/e2e -run '^$' -count=1`、`git diff --check`、`git diff --cached --check`。
- 用户确认后已 push #387 注释对齐 commit `bc8e85b docs: align sandbox pod annotation comment` 到 `origin/feat/agent-sandbox-latest`。该 commit 只修改 `pkg/workloadmanager/handlers.go` 注释：从硬编码 `agents.x-k8s.io/pod-name` 改为引用 `sandboxv1alpha1.SandboxPodNameAnnotation`，用于回应 Copilot 的低风险注释维护性建议。验证通过：`go test ./pkg/workloadmanager -count=1`、`git diff --check`。PR #387 当前 head 为 `bc8e85b`，changed files 仍为 15。
- PR skill 已强化最小修原则：fix/feature/compatibility PR 默认把 upstream base 当作稳定基线，只改解决目标问题必需的最少文件；代码清洁、格式、镜像卫生、注释润色、无关 refactor 不应夹带在功能/修复 PR 中，目标测试不依赖的 cleanup 必须移除或另开 cleanup PR。
- 已调整 fork 分支语义：`origin/main` 已 force-with-lease 重置为 `upstream/main bed6bd4` 的干净镜像；所有实习报告、TODO、local skills 和中文记录保存在 `origin/intern`。本地当前应在 `intern` 上继续做记录，upstream PR topic branches 继续从 `upstream/main` 创建。
- Day20 已更名为 [AgentCube 项目梳理与 agent-sandbox v0.2/v0.3/v0.5 WIP PR 实现记录](internship-reports/day20-agent-sandbox-v02-v03-v05-wip-pr-implementations-and-project-study.md)。核心结论：当前 main 是 Go `1.26.4` + `agent-sandbox v0.1.1`；设计文档中的 `Ready -> Paused -> Ready` 尚未实现，当前真实语义是 Router/Store last_activity + WorkloadManager/AgentD delete-on-idle。新增 `agent-sandbox v0.2.1` / `v0.3.10` / `v0.5.0rc1` 三段 WIP PR / validation PR 记录：0.2 是 dependency-only，0.3 开始需要 claim adopted Sandbox、public annotation、NetworkPolicy opt-out、codegen/generated 闭环，0.5 是 v1beta1 API migration。

## Current Blockers

- Day21 只是源码和官方文档调研，未部署 OpenSandbox / Agent Substrate：OpenSandbox K8s 路径需要 controller/CRD/registry/RuntimeClass；Agent Substrate 需要 K8s、ValKey/Redis、gVisor/runsc、对象存储和多组件控制面。
- 当前机器没有 `/dev/kvm`，CPU 虚拟化 flags 未暴露，不能实测 Kuasar / MicroVM / forkd / CubeSandbox 的真实虚拟化路径。
- kind 标准 Kubernetes 在本机 kubelet cgroup/QoS 初始化处失败；KWOK 只能用于调度语义，不等同完整 K8s 实测。
- kind 标准集群创建仍在本机 kubelet/cgroup 环境处失败；Day16 真实 runtime 验证改用已有 k3s。不能把 kind 失败描述成 AgentCube 代码失败。
- PR #385 当前主要等待 maintainer review、`/lgtm`、`/approve` 和 tide 合并门禁。
- PR #387 当前不应直接把 `v0.5.0rc1` 扩入同一 PR；它需要 v1beta1 API / claim `WarmPoolRef` / Sandbox `OperatingMode` 语义迁移，已拆到 Day18 前沿适配测试。PR body 和 label 已更新；后续解释性 comment 由用户自己发，除非用户另行要求。
- 全量 `go test ./...` 会因本机未启动 Router/WorkloadManager、无 kubeconfig 而在 `test/e2e` 失败；排除 e2e 的全量 Go 测试已通过。系统默认 `go` 已修复为 Go 1.26.4，不再是 blocker。
- `agent-sandbox v0.3.10` 适配实验已补 codegen 闭环并通过 `make gen-check`。注意不要在同一 worktree 并行跑 `make gen-check` 和 `make build-all`，二者都会执行 generate / `go mod tidy`，并行会产生临时中间态失败；串行重跑通过。

## Ruled Out

- 不再把 `WarmPoolNotFound` 当成需要 Warning Event 的稳定故障；它可能只是 controller 创建 warm pool 后 cache 尚未同步。
- 不重复评论 #379 里 Copilot 已经指出的 `ctrl.SetupSignalHandler()` 双调用问题。
- 不声称本机跑通过真实 SnapStart restore；当前只能做代码阅读、controller/unit test 级验证。
- 不把 `v0.5.0rc1` 不适配的原因简化成“pseudo-version”。真实阻塞是 `v1alpha1` package 缺失、direct Sandbox `Replicas` 字段移除、SandboxClaim `TemplateRef` 语义改成 required `WarmPoolRef`。

## Next

- Day35 架构后续：不要把三期架构一次性开成大 PR。若要推进 upstream，先拆 design proposal / RuntimeProvider 与 node-ctl contract / SandboxPoolTemplate+SandboxPool CRD skeleton / placeholder-agent RuntimeClass spike / cold-warm-restore benchmark suite / E2B SDK conformance matrix。尤其要先验证“占位 Pod 跳过真实 cgroup”是否能和 kubelet eviction、metrics、QoS 正常相容。
- Push CI PR #414 next step：等待 maintainer review、`/lgtm`、`/approve` 和 tide；不要自动 push 新 commit 或发评论。若 maintainer 质疑成本，优先解释这是 Karmada-style shared workflow，push/PR 复用同一 CI 定义；如需改成 E2E PR-only 或调整触发范围，先给用户确认 exact diff/comment。
- Day30/#387 review 后续：如果 reviewer 追问 warm pool adoption，优先用 Day30 的 Mermaid 和英文草稿说明数据流：`SandboxClaim.status.sandbox.name` 是 claim -> serving Sandbox 的关键 bridge；Pod 由 adopted Sandbox annotation / ownerRef 找；Store 同时保留 claim identity 与 runtime identity。注意不要再把 no-warm-pool direct Sandbox 当旧模型对照；direct path 只解释为什么 warm claim 不能复用同名 Sandbox watcher。新增测试方向不是再补黑盒 CodeInterpreter e2e，而是先做 fork/local `warmpool-flow-inspector`，直接观测 CRD/API object flow、UID continuity、ownerRef/status 转移和 warm pool refill。2026-06-25 已按用户要求补跑真正 #387 版本的 live breakpoint test：`/home/agentcube-agent-sandbox-latest` branch `rebase/pr387-on-bed6bd4` head `c2633c5` 本地 WorkloadManager/Router + `agent-sandbox-controller:v0.4.6` + `CodeInterpreter/pr387-math-ci`，`trace_math_dataflow_breakpoints.py` BP01-BP09 全过，`SandboxClaim/pr387-math-ci-j4gbks6g.status.sandbox.name=pr387-math-ci-4fzgd`，Router/PicoD 返回数学答案 2，delete cleanup/refill 通过；摘要在 `internship-reports/benchmarks/day30-pr387-warmpool-flow/14-pr387-live-codeinterpreter-breakpoint-result.txt`。长驻集群已恢复到原 AgentCube deployments 和 v0.1.1 StatefulSet。不要主动发 upstream comment；如需评论或开测试工具 PR，先让用户确认英文全文和范围。不要把 `agent-sandbox v0.5.x`、Sleep/Resume 或 PicoD cleanup 混进 #387。Pod informer cache 可能短暂滞后是 follow-up 风险，可建议短 retry 或 claim status PodIPs fallback，但不在当前 PR 主动扩大 scope。
- E2B 后续：不要把“E2B-like code interpreter”写成“E2B-compatible”。Day33 已把兼容面拆成 SDK、REST lifecycle、envd process/filesystem RPC、template/snapshot/network/volume。若推进 AgentCube E2B 方向，先写独立 facade/API proposal 和 conformance test plan：official E2B Python/JS SDK create、commands.run、files read/write/list、pause/connect/kill、timeout/error/auth/cleanup；不要混入 #387 agent-sandbox compatibility 或 Sleep/Resume 状态机 PR。
- Day27 后续：把实习周总结从“任务流水账”改成能力闭环：问题定义、源码/文档证据、设计判断、最小实现或复现、分层测试、review 材料、skill 沉淀。下一步优先做 `Session lifecycle contract table`、Sleep/Resume Stage 3 Router/GC 行为表、#394/#395/#397/#388 源码级 review 矩阵和 benchmark schema。
- Day28 后续：如果继续 Substrate 方向，先画 AgentCube future architecture drawio，分当前架构、Sleep/Resume MVP、MultiAgent Worker Pod 三层；或另画 Agent Substrate micro-VM runtime path 图，重点说明 `SandboxConfig` assets、`/dev/kvm`、Kata / Cloud Hypervisor、guest RAM snapshot 和 rootfs reset-to-golden 语义。若写 MultiAgent Worker proposal，重点回答 isolation、slot allocation、workspace/credential separation、resource accounting、failure domain、Router slot routing 和 cleanup tests。暂不发 upstream。
- Day29 后续：跟踪 upstream PR #403 checks、Copilot/Gemini feedback、OWNER/reviewer/tide 状态；不要自动 push 新 commits 或发评论，先分类是否属于 PR scope，再让用户确认 exact update/comment。
- Day24/25 后续：暂不发 upstream。英文 Sleep/Resume proposal 草稿、fake RuntimeProvider + Store CAS 状态机 spike、第一阶段 Store 状态/CAS、第二阶段 WorkloadManager lifecycle service、阶段性代码审查与架构复盘、Stage 3 Router/GC 行为表、todo 子任务拆解和 3A Store/GC split 测试骨架均已完成。下一步如果继续推进，优先做 3B Router resume-before-proxy test skeleton，或把 3A 转成 fork/local validation；3C 先写 API contract；3D 等 #387 合并和维护者分工；3E 等最小实现后跑 e2e/math-agent。
- Day23 后续建议：先写本地 Sleep/Resume design note，不急着发 upstream；从 #387 代码中提炼 `RuntimeProvider` 边界草图；为 `sessionTimeout / pauseTimeout / maxSessionDuration / ttl / delete` 画 API 语义表；设计统一 benchmark schema 覆盖 cold/warm/pause/resume/SnapStart/math-agent/cleanup；继续跟踪 #400/#331/#382/#291，但只有在有代码证据或测试证据时参与社区评论。
- 如果继续 Day21 方向：OpenSandbox 先跑 Docker runtime 最小 smoke，再测 Kubernetes `BatchSandbox` / `agent-sandbox` provider；Agent Substrate 先跑 kind/GKE quickstart + counter demo。AgentBay / AWS AgentCore 更偏托管产品和 SDK，后续应另开产品对比报告。
- User confirmed updating #387. `origin/feat/agent-sandbox-latest` was force-with-lease pushed from `bc8e85b` to `c2633c5` using local branch `/home/agentcube-agent-sandbox-latest` `rebase/pr387-on-bed6bd4`. PR #387 now lists 5 commits (`bacf12d`, `1272052`, `1cb73f7`, `5ffc44b`, `c2633c5`) and 15 changed files. This should resolve tide's merge conflict caused by `upstream/main bed6bd4`; wait for refreshed CI/tide before further action. Do not post comments or push more updates without user confirmation.
- Branch hygiene: keep fork `main` as a clean mirror of `upstream/main`; do not put internship reports or Chinese notes there. Continue local records on `intern`; rebase `intern` onto `upstream/main` when project code needs to be current.
- Day20 后续：如继续做架构学习，下一步专项深读 Python SDK / CLI auth/session 封装，或补 agent-sandbox controller 源码状态机。0.2/0.3 分段适配实验已创建 fork-only 归档 PR 并全绿：#6 `agent-sandbox v0.2.1` head `a4506d6`，#7 `agent-sandbox v0.3.10` head `142c56e`。#7 第一次 fork CI 暴露 `golangci-lint` 问题，已用 `142c56e test: fix v0.3 adaptation lint` 修复。不要把 Day20 的 general notes 混入 upstream PR。
- Day18 `agent-sandbox v0.5.x` next step: PR #5 has been rebased onto latest #387 head and is green. PR #5 base `release-agent-sandbox-v05-base -> c2633c5`, head `test/agent-sandbox-v05-forward -> 3abdb94`; local post-rebase validation passed (`go test` target packages, non-e2e all Go tests, `make lint`, e2e static, `make gen-check`, `make build-all`, diff checks) and fork CI 10/10 passed. Runtime evidence is still from the prior clean rc1/v1beta1 run; no upstream PR should be opened until official `v0.5.0` release or explicit maintainer request for rc support.
- 根据 2026-06-17 例会纪要，把 `Sleep/Resume`、E2B-compatible API / SDK / template 分别拆成可公开讨论的英文 proposal / issue comment；`agent-sandbox` 适配已进入代码分支推进。
- 与 FAUST-BENCHOU 讨论 Sleep/Resume 时优先确认第一版语义：是否接受基于 `Sandbox.spec.replicas=0/1` 的 stop/recreate，`context` 是否仅指 workspace/PVC，是否新增 `pauseTimeout`，warm-pool-backed CodeInterpreter 是否纳入第一版。
- 建议把 #386 两个方向作为同一 v0.2.0 epic 的两个子任务讨论：先定 agent-sandbox target version / compatibility foundation，再做 AgentCube Sleep/Resume lifecycle。
- 跟踪 #386 maintainer 是否 triage agent-sandbox 适配为 dedicated sub-issue；有明确方向后再准备 compatibility audit PR 或最小修复 PR。
- 后续看 issue/PR 时先跑 `thread_brief.py <number>`；看 PR 再配合 `pr_status.py <number>`，如果流程重复就继续补脚本而不是手工重做。
- 优先把 #379 的 `ReadyAt` status equality 评论发到 PR，或先在临时分支补最小 controller unit test 验证。
- 继续跟踪 #385 的 CI、Codecov、tide、maintainer review，有反馈再小步处理。
- 如果继续做 benchmark，只跑当前环境能支撑的 AgentCube / k3s / controller 层测试，并明确“不包含真实 MicroVM/KVM restore”。

## Stop Conditions

- 同一个环境问题连续失败 3 次，例如 kind kubelet cgroup/QoS 或 `/dev/kvm` 缺失，不再硬调，转为记录 BLOCKED 并换机器/环境。
- 同一个 upstream PR 已有人明确认领并正在改同一问题，不开重复 PR，只做 review、复现或测试反馈。
- 如果只能基于推测发社区评论，先停止，补代码证据、测试证据或引用具体文件行后再发。
